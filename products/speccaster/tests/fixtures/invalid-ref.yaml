openapi: 3.0.3
info:
  title: Invalid refs
  version: 1.0.0
paths:
  /broken:
    get:
      operationId: broken
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DoesNotExist'
components:
  schemas:
    Real:
      type: string