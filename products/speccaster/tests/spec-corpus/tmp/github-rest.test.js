// Generated by speccaster v0.2.0 — your suite, extend it freely.
// Regenerate from your latest spec, then the drift gate keeps spec / suite in sync:
//   npx speccaster init --spec <spec> --out <file> --force
// These node:test cases are committed to your repo on purpose.

const { test, describe } = require('node:test');
const assert = require('node:assert/strict');

const BASE = process.env.SPECCASTER_BASE_URL || "https://api.github.com";

describe("meta / GET /", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("security-advisories / GET /advisories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/advisories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /advisories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("security-advisories / GET /advisories/{ghsa_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/advisories/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /advisories/{ghsa_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agent-tasks / GET /agents/repos/{owner}/{repo}/tasks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/agents/repos/sample/sample/tasks`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /agents/repos/{owner}/{repo}/tasks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agent-tasks / POST /agents/repos/{owner}/{repo}/tasks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/agents/repos/sample/sample/tasks`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"prompt\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /agents/repos/{owner}/{repo}/tasks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agent-tasks / GET /agents/repos/{owner}/{repo}/tasks/{task_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/agents/repos/sample/sample/tasks/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /agents/repos/{owner}/{repo}/tasks/{task_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agent-tasks / GET /agents/tasks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/agents/tasks`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /agents/tasks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agent-tasks / GET /agents/tasks/{task_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/agents/tasks/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /agents/tasks/{task_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /app", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/app`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /app');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / POST /app-manifests/{code}/conversions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/app-manifests/sample/conversions`, {
      method: "POST",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /app-manifests/{code}/conversions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /app/hook/config", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/app/hook/config`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /app/hook/config');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / PATCH /app/hook/config", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/app/hook/config`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"url\":\"sample\",\"content_type\":\"sample\",\"secret\":\"sample\",\"insecure_ssl\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /app/hook/config');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /app/hook/deliveries", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/app/hook/deliveries`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /app/hook/deliveries');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /app/hook/deliveries/{delivery_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/app/hook/deliveries/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /app/hook/deliveries/{delivery_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / POST /app/hook/deliveries/{delivery_id}/attempts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/app/hook/deliveries/sample/attempts`, {
      method: "POST",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /app/hook/deliveries/{delivery_id}/attempts');
  });
});

describe("apps / GET /app/installation-requests", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/app/installation-requests`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /app/installation-requests');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /app/installations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/app/installations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /app/installations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /app/installations/{installation_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/app/installations/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /app/installations/{installation_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / DELETE /app/installations/{installation_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/app/installations/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /app/installations/{installation_id}');
  });
});

describe("apps / POST /app/installations/{installation_id}/access_tokens", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/app/installations/sample/access_tokens`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"repositories\":[\"rails\"],\"repository_ids\":[1],\"permissions\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /app/installations/{installation_id}/access_tokens');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / PUT /app/installations/{installation_id}/suspended", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/app/installations/sample/suspended`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /app/installations/{installation_id}/suspended');
  });
});

describe("apps / DELETE /app/installations/{installation_id}/suspended", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/app/installations/sample/suspended`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /app/installations/{installation_id}/suspended');
  });
});

describe("apps / DELETE /applications/{client_id}/grant", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/applications/sample/grant`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"access_token\":\"sample\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /applications/{client_id}/grant');
  });
});

describe("apps / POST /applications/{client_id}/token", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/applications/sample/token`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"access_token\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /applications/{client_id}/token');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / DELETE /applications/{client_id}/token", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/applications/sample/token`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"access_token\":\"sample\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /applications/{client_id}/token');
  });
});

describe("apps / PATCH /applications/{client_id}/token", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/applications/sample/token`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"access_token\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /applications/{client_id}/token');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / POST /applications/{client_id}/token/scoped", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/applications/sample/token/scoped`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"access_token\":\"e72e16c7e42f292c6912e7710c838347ae178b4a\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /applications/{client_id}/token/scoped');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /apps/{app_slug}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/apps/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /apps/{app_slug}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("classroom / GET /assignments/{assignment_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/assignments/sample`, {
      method: "GET",
    });
    const okCodes = ["410"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /assignments/{assignment_id}');
  });
});

describe("classroom / GET /assignments/{assignment_id}/accepted_assignments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/assignments/sample/accepted_assignments`, {
      method: "GET",
    });
    const okCodes = ["410"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /assignments/{assignment_id}/accepted_assignments');
  });
});

describe("classroom / GET /assignments/{assignment_id}/grades", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/assignments/sample/grades`, {
      method: "GET",
    });
    const okCodes = ["410"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /assignments/{assignment_id}/grades');
  });
});

describe("classroom / GET /classrooms", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/classrooms`, {
      method: "GET",
    });
    const okCodes = ["410"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /classrooms');
  });
});

describe("classroom / GET /classrooms/{classroom_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/classrooms/sample`, {
      method: "GET",
    });
    const okCodes = ["410"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /classrooms/{classroom_id}');
  });
});

describe("classroom / GET /classrooms/{classroom_id}/assignments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/classrooms/sample/assignments`, {
      method: "GET",
    });
    const okCodes = ["410"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /classrooms/{classroom_id}/assignments');
  });
});

describe("codes-of-conduct / GET /codes_of_conduct", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/codes_of_conduct`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /codes_of_conduct');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codes-of-conduct / GET /codes_of_conduct/{key}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/codes_of_conduct/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /codes_of_conduct/{key}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("credentials / POST /credentials/revoke", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/credentials/revoke`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"credentials\":[\"sample\"]}",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /credentials/revoke');
  });
});

describe("emojis / GET /emojis", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/emojis`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /emojis');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /enterprises/{enterprise}/actions/cache/retention-limit", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/actions/cache/retention-limit`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/actions/cache/retention-limit');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /enterprises/{enterprise}/actions/cache/retention-limit", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/actions/cache/retention-limit`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"max_cache_retention_days\":14}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /enterprises/{enterprise}/actions/cache/retention-limit');
  });
});

describe("actions / GET /enterprises/{enterprise}/actions/cache/storage-limit", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/actions/cache/storage-limit`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/actions/cache/storage-limit');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /enterprises/{enterprise}/actions/cache/storage-limit", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/actions/cache/storage-limit`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"max_cache_size_gb\":10}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /enterprises/{enterprise}/actions/cache/storage-limit');
  });
});

describe("oidc / GET /enterprises/{enterprise}/actions/oidc/customization/properties/repo", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/actions/oidc/customization/properties/repo`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/actions/oidc/customization/properties/repo');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("oidc / POST /enterprises/{enterprise}/actions/oidc/customization/properties/repo", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/actions/oidc/customization/properties/repo`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"custom_property_name\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /enterprises/{enterprise}/actions/oidc/customization/properties/repo');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("oidc / DELETE /enterprises/{enterprise}/actions/oidc/customization/properties/repo/{custom_property_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/actions/oidc/customization/properties/repo/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /enterprises/{enterprise}/actions/oidc/customization/properties/repo/{custom_property_name}');
  });
});

describe("code-security / GET /enterprises/{enterprise}/code-security/configurations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/code-security/configurations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/code-security/configurations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-security / POST /enterprises/{enterprise}/code-security/configurations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/code-security/configurations`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /enterprises/{enterprise}/code-security/configurations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-security / GET /enterprises/{enterprise}/code-security/configurations/defaults", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/code-security/configurations/defaults`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/code-security/configurations/defaults');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-security / GET /enterprises/{enterprise}/code-security/configurations/{configuration_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/code-security/configurations/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/code-security/configurations/{configuration_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-security / DELETE /enterprises/{enterprise}/code-security/configurations/{configuration_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/code-security/configurations/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /enterprises/{enterprise}/code-security/configurations/{configuration_id}');
  });
});

describe("code-security / PATCH /enterprises/{enterprise}/code-security/configurations/{configuration_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/code-security/configurations/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"description\":\"sample\",\"advanced_security\":\"enabled\",\"code_security\":\"enabled\",\"dependency_graph\":\"enabled\",\"dependency_graph_autosubmit_action\":\"enabled\",\"dependency_graph_autosubmit_action_options\":{\"labeled_runners\":true},\"dependabot_alerts\":\"enabled\",\"dependabot_security_updates\":\"enabled\",\"code_scanning_default_setup\":\"enabled\",\"code_scanning_default_setup_options\":\"sample\",\"code_scanning_options\":\"sample\",\"code_scanning_delegated_alert_dismissal\":\"disabled\",\"secret_protection\":\"enabled\",\"secret_scanning\":\"enabled\",\"secret_scanning_push_protection\":\"enabled\",\"secret_scanning_validity_checks\":\"enabled\",\"secret_scanning_non_provider_patterns\":\"enabled\",\"secret_scanning_generic_secrets\":\"disabled\",\"secret_scanning_delegated_alert_dismissal\":\"disabled\",\"secret_scanning_extended_metadata\":\"disabled\",\"private_vulnerability_reporting\":\"enabled\",\"enforcement\":\"enforced\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /enterprises/{enterprise}/code-security/configurations/{configuration_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-security / POST /enterprises/{enterprise}/code-security/configurations/{configuration_id}/attach", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/code-security/configurations/sample/attach`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"scope\":\"all\"}",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /enterprises/{enterprise}/code-security/configurations/{configuration_id}/attach');
  });
});

describe("code-security / PUT /enterprises/{enterprise}/code-security/configurations/{configuration_id}/defaults", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/code-security/configurations/sample/defaults`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"default_for_new_repos\":\"all\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /enterprises/{enterprise}/code-security/configurations/{configuration_id}/defaults');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-security / GET /enterprises/{enterprise}/code-security/configurations/{configuration_id}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/code-security/configurations/sample/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/code-security/configurations/{configuration_id}/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / GET /enterprises/{enterprise}/copilot/metrics/reports/enterprise-1-day", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/copilot/metrics/reports/enterprise-1-day`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/copilot/metrics/reports/enterprise-1-day');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / GET /enterprises/{enterprise}/copilot/metrics/reports/enterprise-28-day/latest", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/copilot/metrics/reports/enterprise-28-day/latest`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/copilot/metrics/reports/enterprise-28-day/latest');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / GET /enterprises/{enterprise}/copilot/metrics/reports/repos-1-day", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/copilot/metrics/reports/repos-1-day`, {
      method: "GET",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/copilot/metrics/reports/repos-1-day');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / GET /enterprises/{enterprise}/copilot/metrics/reports/user-teams-1-day", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/copilot/metrics/reports/user-teams-1-day`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/copilot/metrics/reports/user-teams-1-day');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / GET /enterprises/{enterprise}/copilot/metrics/reports/users-1-day", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/copilot/metrics/reports/users-1-day`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/copilot/metrics/reports/users-1-day');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / GET /enterprises/{enterprise}/copilot/metrics/reports/users-28-day/latest", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/copilot/metrics/reports/users-28-day/latest`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/copilot/metrics/reports/users-28-day/latest');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / PUT /enterprises/{enterprise}/copilot/policies/coding_agent", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/copilot/policies/coding_agent`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"policy_state\":\"enabled_for_all_orgs\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /enterprises/{enterprise}/copilot/policies/coding_agent');
  });
});

describe("copilot / POST /enterprises/{enterprise}/copilot/policies/coding_agent/organizations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/copilot/policies/coding_agent/organizations`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"organizations\":[\"sample\"],\"custom_properties\":[{\"property_name\":\"sample\",\"values\":[\"sample\"]}]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /enterprises/{enterprise}/copilot/policies/coding_agent/organizations');
  });
});

describe("copilot / DELETE /enterprises/{enterprise}/copilot/policies/coding_agent/organizations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/copilot/policies/coding_agent/organizations`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"organizations\":[\"sample\"],\"custom_properties\":[{\"property_name\":\"sample\",\"values\":[\"sample\"]}]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /enterprises/{enterprise}/copilot/policies/coding_agent/organizations');
  });
});

describe("dependabot / GET /enterprises/{enterprise}/dependabot/alerts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/dependabot/alerts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/dependabot/alerts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / GET /enterprises/{enterprise}/dependabot/repository-access", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/dependabot/repository-access`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/dependabot/repository-access');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / PATCH /enterprises/{enterprise}/dependabot/repository-access", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/dependabot/repository-access`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"repository_ids_to_add\":[123,456],\"repository_ids_to_remove\":[789]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /enterprises/{enterprise}/dependabot/repository-access');
  });
});

describe("dependabot / PUT /enterprises/{enterprise}/dependabot/repository-access/default-level", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/dependabot/repository-access/default-level`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"default_level\":\"internal\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /enterprises/{enterprise}/dependabot/repository-access/default-level');
  });
});

describe("enterprise-team-memberships / GET /enterprises/{enterprise}/members/{username}/teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/members/sample/teams`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/members/{username}/teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("enterprise-teams / GET /enterprises/{enterprise}/teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("enterprise-teams / POST /enterprises/{enterprise}/teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /enterprises/{enterprise}/teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("enterprise-team-memberships / GET /enterprises/{enterprise}/teams/{enterprise-team}/memberships", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams/sample/memberships`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/teams/{enterprise-team}/memberships');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("enterprise-team-memberships / POST /enterprises/{enterprise}/teams/{enterprise-team}/memberships/add", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams/sample/memberships/add`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"usernames\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /enterprises/{enterprise}/teams/{enterprise-team}/memberships/add');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("enterprise-team-memberships / POST /enterprises/{enterprise}/teams/{enterprise-team}/memberships/remove", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams/sample/memberships/remove`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"usernames\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /enterprises/{enterprise}/teams/{enterprise-team}/memberships/remove');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("enterprise-team-memberships / GET /enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams/sample/memberships/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("enterprise-team-memberships / PUT /enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams/sample/memberships/sample`, {
      method: "PUT",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("enterprise-team-memberships / DELETE /enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams/sample/memberships/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username}');
  });
});

describe("enterprise-team-organizations / GET /enterprises/{enterprise}/teams/{enterprise-team}/organizations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams/sample/organizations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/teams/{enterprise-team}/organizations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("enterprise-team-organizations / POST /enterprises/{enterprise}/teams/{enterprise-team}/organizations/add", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams/sample/organizations/add`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"organization_slugs\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /enterprises/{enterprise}/teams/{enterprise-team}/organizations/add');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("enterprise-team-organizations / POST /enterprises/{enterprise}/teams/{enterprise-team}/organizations/remove", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams/sample/organizations/remove`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"organization_slugs\":[\"sample\"]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /enterprises/{enterprise}/teams/{enterprise-team}/organizations/remove');
  });
});

describe("enterprise-team-organizations / GET /enterprises/{enterprise}/teams/{enterprise-team}/organizations/{org}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams/sample/organizations/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/teams/{enterprise-team}/organizations/{org}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("enterprise-team-organizations / PUT /enterprises/{enterprise}/teams/{enterprise-team}/organizations/{org}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams/sample/organizations/sample`, {
      method: "PUT",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /enterprises/{enterprise}/teams/{enterprise-team}/organizations/{org}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("enterprise-team-organizations / DELETE /enterprises/{enterprise}/teams/{enterprise-team}/organizations/{org}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams/sample/organizations/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /enterprises/{enterprise}/teams/{enterprise-team}/organizations/{org}');
  });
});

describe("enterprise-teams / GET /enterprises/{enterprise}/teams/{team_slug}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /enterprises/{enterprise}/teams/{team_slug}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("enterprise-teams / DELETE /enterprises/{enterprise}/teams/{team_slug}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /enterprises/{enterprise}/teams/{team_slug}');
  });
});

describe("enterprise-teams / PATCH /enterprises/{enterprise}/teams/{team_slug}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/enterprises/sample/teams/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"description\":\"sample\",\"sync_to_organizations\":\"disabled\",\"organization_selection_type\":\"disabled\",\"group_id\":\"sample\",\"notification_setting\":\"notifications_enabled\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /enterprises/{enterprise}/teams/{team_slug}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /events", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/events`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /events');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /feeds", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/feeds`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /feeds');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("gists / GET /gists", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /gists');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("gists / POST /gists", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"files\":{\"hello.rb\":{\"content\":\"puts \\\"Hello, World!\\\"\"}}}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /gists');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("gists / GET /gists/public", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/public`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /gists/public');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("gists / GET /gists/starred", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/starred`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /gists/starred');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("gists / GET /gists/{gist_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /gists/{gist_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("gists / DELETE /gists/{gist_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /gists/{gist_id}');
  });
});

describe("gists / PATCH /gists/{gist_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"description\":\"Example Ruby script\",\"files\":{\"hello.rb\":{\"content\":\"blah\",\"filename\":\"goodbye.rb\"}}}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /gists/{gist_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("gists / GET /gists/{gist_id}/comments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/sample/comments`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /gists/{gist_id}/comments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("gists / POST /gists/{gist_id}/comments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/sample/comments`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"body\":\"Body of the attachment\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /gists/{gist_id}/comments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("gists / GET /gists/{gist_id}/comments/{comment_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/sample/comments/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /gists/{gist_id}/comments/{comment_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("gists / DELETE /gists/{gist_id}/comments/{comment_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/sample/comments/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /gists/{gist_id}/comments/{comment_id}');
  });
});

describe("gists / PATCH /gists/{gist_id}/comments/{comment_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/sample/comments/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"body\":\"Body of the attachment\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /gists/{gist_id}/comments/{comment_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("gists / GET /gists/{gist_id}/commits", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/sample/commits`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /gists/{gist_id}/commits');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("gists / GET /gists/{gist_id}/forks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/sample/forks`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /gists/{gist_id}/forks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("gists / POST /gists/{gist_id}/forks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/sample/forks`, {
      method: "POST",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /gists/{gist_id}/forks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("gists / GET /gists/{gist_id}/star", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/sample/star`, {
      method: "GET",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /gists/{gist_id}/star');
  });
});

describe("gists / PUT /gists/{gist_id}/star", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/sample/star`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /gists/{gist_id}/star');
  });
});

describe("gists / DELETE /gists/{gist_id}/star", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/sample/star`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /gists/{gist_id}/star');
  });
});

describe("gists / GET /gists/{gist_id}/{sha}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gists/sample/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /gists/{gist_id}/{sha}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("gitignore / GET /gitignore/templates", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gitignore/templates`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /gitignore/templates');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("gitignore / GET /gitignore/templates/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/gitignore/templates/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /gitignore/templates/{name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /installation/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/installation/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /installation/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / DELETE /installation/token", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/installation/token`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /installation/token');
  });
});

describe("issues / GET /issues", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/issues`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /issues');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("licenses / GET /licenses", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/licenses`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /licenses');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("licenses / GET /licenses/{license}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/licenses/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /licenses/{license}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("markdown / POST /markdown", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/markdown`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"text\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /markdown');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("text/"), 'expected text/html, got ' + ct.split(';')[0]);
  });
});

describe("markdown / POST /markdown/raw", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/markdown/raw`, {
      method: "POST",
      headers: { 'content-type': "text/plain" },
      body: "\"sample\"",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /markdown/raw');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("text/"), 'expected text/html, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /marketplace_listing/accounts/{account_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/marketplace_listing/accounts/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /marketplace_listing/accounts/{account_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /marketplace_listing/plans", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/marketplace_listing/plans`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /marketplace_listing/plans');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /marketplace_listing/plans/{plan_id}/accounts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/marketplace_listing/plans/sample/accounts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /marketplace_listing/plans/{plan_id}/accounts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /marketplace_listing/stubbed/accounts/{account_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/marketplace_listing/stubbed/accounts/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /marketplace_listing/stubbed/accounts/{account_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /marketplace_listing/stubbed/plans", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/marketplace_listing/stubbed/plans`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /marketplace_listing/stubbed/plans');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /marketplace_listing/stubbed/plans/{plan_id}/accounts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/marketplace_listing/stubbed/plans/sample/accounts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /marketplace_listing/stubbed/plans/{plan_id}/accounts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("meta / GET /meta", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/meta`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /meta');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /networks/{owner}/{repo}/events", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/networks/sample/sample/events`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /networks/{owner}/{repo}/events');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /notifications", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/notifications`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /notifications');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / PUT /notifications", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/notifications`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"last_read_at\":\"2026-01-01T00:00:00Z\",\"read\":true}",
    });
    const okCodes = ["202","205"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /notifications');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /notifications/threads/{thread_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/notifications/threads/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /notifications/threads/{thread_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / DELETE /notifications/threads/{thread_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/notifications/threads/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /notifications/threads/{thread_id}');
  });
});

describe("activity / PATCH /notifications/threads/{thread_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/notifications/threads/sample`, {
      method: "PATCH",
    });
    const okCodes = ["205"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /notifications/threads/{thread_id}');
  });
});

describe("activity / GET /notifications/threads/{thread_id}/subscription", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/notifications/threads/sample/subscription`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /notifications/threads/{thread_id}/subscription');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / PUT /notifications/threads/{thread_id}/subscription", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/notifications/threads/sample/subscription`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"ignored\":false}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /notifications/threads/{thread_id}/subscription');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / DELETE /notifications/threads/{thread_id}/subscription", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/notifications/threads/sample/subscription`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /notifications/threads/{thread_id}/subscription');
  });
});

describe("meta / GET /octocat", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/octocat`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /octocat');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/octocat-stream, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /organizations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/organizations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /organizations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /organizations/{org}/actions/cache/retention-limit", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/organizations/sample/actions/cache/retention-limit`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /organizations/{org}/actions/cache/retention-limit');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /organizations/{org}/actions/cache/retention-limit", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/organizations/sample/actions/cache/retention-limit`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"max_cache_retention_days\":14}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /organizations/{org}/actions/cache/retention-limit');
  });
});

describe("actions / GET /organizations/{org}/actions/cache/storage-limit", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/organizations/sample/actions/cache/storage-limit`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /organizations/{org}/actions/cache/storage-limit');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /organizations/{org}/actions/cache/storage-limit", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/organizations/sample/actions/cache/storage-limit`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"max_cache_size_gb\":10}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /organizations/{org}/actions/cache/storage-limit');
  });
});

describe("billing / GET /organizations/{org}/settings/billing/ai_credit/usage", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/organizations/sample/settings/billing/ai_credit/usage`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /organizations/{org}/settings/billing/ai_credit/usage');
  });
});

describe("billing / GET /organizations/{org}/settings/billing/budgets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/organizations/sample/settings/billing/budgets`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /organizations/{org}/settings/billing/budgets');
  });
});

describe("billing / POST /organizations/{org}/settings/billing/budgets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/organizations/sample/settings/billing/budgets`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"budget_amount\":1,\"prevent_further_usage\":true,\"budget_alerting\":{\"will_alert\":true,\"alert_recipients\":[\"sample\"]},\"budget_scope\":\"organization\",\"budget_entity_name\":\"\",\"budget_type\":\"BundlePricing\",\"budget_product_sku\":\"sample\",\"user\":\"sample\",\"expires_at\":\"2026-01-01\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /organizations/{org}/settings/billing/budgets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("billing / GET /organizations/{org}/settings/billing/budgets/{budget_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/organizations/sample/settings/billing/budgets/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /organizations/{org}/settings/billing/budgets/{budget_id}');
  });
});

describe("billing / DELETE /organizations/{org}/settings/billing/budgets/{budget_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/organizations/sample/settings/billing/budgets/sample`, {
      method: "DELETE",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /organizations/{org}/settings/billing/budgets/{budget_id}');
  });
});

describe("billing / PATCH /organizations/{org}/settings/billing/budgets/{budget_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/organizations/sample/settings/billing/budgets/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"budget_amount\":1,\"prevent_further_usage\":true,\"budget_alerting\":{\"will_alert\":true,\"alert_recipients\":[\"sample\"]},\"budget_scope\":\"enterprise\",\"budget_entity_name\":\"sample\",\"budget_type\":\"BundlePricing\",\"budget_product_sku\":\"sample\",\"user\":\"sample\",\"expires_at\":\"2026-01-01\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /organizations/{org}/settings/billing/budgets/{budget_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("billing / GET /organizations/{org}/settings/billing/premium_request/usage", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/organizations/sample/settings/billing/premium_request/usage`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /organizations/{org}/settings/billing/premium_request/usage');
  });
});

describe("billing / GET /organizations/{org}/settings/billing/usage", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/organizations/sample/settings/billing/usage`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /organizations/{org}/settings/billing/usage');
  });
});

describe("billing / GET /organizations/{org}/settings/billing/usage/summary", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/organizations/sample/settings/billing/usage/summary`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /organizations/{org}/settings/billing/usage/summary');
  });
});

describe("orgs / GET /orgs/{org}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / DELETE /orgs/{org}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample`, {
      method: "DELETE",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}');
  });
});

describe("orgs / PATCH /orgs/{org}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"billing_email\":\"sample\",\"company\":\"sample\",\"email\":\"sample\",\"twitter_username\":\"sample\",\"location\":\"sample\",\"name\":\"sample\",\"description\":\"sample\",\"has_organization_projects\":true,\"has_repository_projects\":true,\"default_repository_permission\":\"read\",\"members_can_create_repositories\":true,\"members_can_create_internal_repositories\":true,\"members_can_create_private_repositories\":true,\"members_can_create_public_repositories\":true,\"members_allowed_repository_creation_type\":\"all\",\"members_can_create_pages\":true,\"members_can_create_public_pages\":true,\"members_can_create_private_pages\":true,\"members_can_fork_private_repositories\":false,\"web_commit_signoff_required\":false,\"blog\":\"\\\"http://github.blog\\\"\",\"advanced_security_enabled_for_new_repositories\":true,\"dependabot_alerts_enabled_for_new_repositories\":true,\"dependabot_security_updates_enabled_for_new_repositories\":true,\"dependency_graph_enabled_for_new_repositories\":true,\"secret_scanning_enabled_for_new_repositories\":true,\"secret_scanning_push_protection_enabled_for_new_repositories\":true,\"secret_scanning_push_protection_custom_link_enabled\":true,\"secret_scanning_push_protection_custom_link\":\"sample\",\"deploy_keys_enabled_for_repositories\":true}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/cache/usage", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/cache/usage`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/cache/usage');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/cache/usage-by-repository", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/cache/usage-by-repository`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/cache/usage-by-repository');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/hosted-runners", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/hosted-runners`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/hosted-runners');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / POST /orgs/{org}/actions/hosted-runners", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/hosted-runners`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"image\":{\"id\":\"sample\",\"source\":\"github\",\"version\":\"sample\"},\"size\":\"sample\",\"runner_group_id\":1}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/actions/hosted-runners');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/hosted-runners/images/custom", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/hosted-runners/images/custom`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/hosted-runners/images/custom');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/hosted-runners/images/custom/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / DELETE /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/hosted-runners/images/custom/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}');
  });
});

describe("actions / GET /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}/versions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/hosted-runners/images/custom/sample/versions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}/versions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}/versions/{version}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/hosted-runners/images/custom/sample/versions/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}/versions/{version}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / DELETE /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}/versions/{version}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/hosted-runners/images/custom/sample/versions/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/actions/hosted-runners/images/custom/{image_definition_id}/versions/{version}');
  });
});

describe("actions / GET /orgs/{org}/actions/hosted-runners/images/github-owned", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/hosted-runners/images/github-owned`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/hosted-runners/images/github-owned');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/hosted-runners/images/partner", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/hosted-runners/images/partner`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/hosted-runners/images/partner');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/hosted-runners/limits", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/hosted-runners/limits`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/hosted-runners/limits');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/hosted-runners/machine-sizes", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/hosted-runners/machine-sizes`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/hosted-runners/machine-sizes');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/hosted-runners/platforms", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/hosted-runners/platforms`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/hosted-runners/platforms');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/hosted-runners/{hosted_runner_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/hosted-runners/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/hosted-runners/{hosted_runner_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / DELETE /orgs/{org}/actions/hosted-runners/{hosted_runner_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/hosted-runners/sample`, {
      method: "DELETE",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/actions/hosted-runners/{hosted_runner_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PATCH /orgs/{org}/actions/hosted-runners/{hosted_runner_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/hosted-runners/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"runner_group_id\":1,\"maximum_runners\":1,\"enable_static_ip\":true,\"size\":\"sample\",\"image_source\":\"github\",\"image_id\":\"sample\",\"image_version\":\"sample\",\"image_gen\":true}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/actions/hosted-runners/{hosted_runner_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("oidc / GET /orgs/{org}/actions/oidc/customization/properties/repo", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/oidc/customization/properties/repo`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/oidc/customization/properties/repo');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("oidc / POST /orgs/{org}/actions/oidc/customization/properties/repo", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/oidc/customization/properties/repo`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"custom_property_name\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/actions/oidc/customization/properties/repo');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("oidc / DELETE /orgs/{org}/actions/oidc/customization/properties/repo/{custom_property_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/oidc/customization/properties/repo/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/actions/oidc/customization/properties/repo/{custom_property_name}');
  });
});

describe("oidc / GET /orgs/{org}/actions/oidc/customization/sub", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/oidc/customization/sub`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/oidc/customization/sub');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("oidc / PUT /orgs/{org}/actions/oidc/customization/sub", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/oidc/customization/sub`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"include_claim_keys\":[\"sample\"],\"use_immutable_subject\":true}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/oidc/customization/sub');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/permissions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/permissions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /orgs/{org}/actions/permissions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"enabled_repositories\":\"sample\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/permissions');
  });
});

describe("actions / GET /orgs/{org}/actions/permissions/artifact-and-log-retention", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/artifact-and-log-retention`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/permissions/artifact-and-log-retention');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /orgs/{org}/actions/permissions/artifact-and-log-retention", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/artifact-and-log-retention`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"days\":1}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/permissions/artifact-and-log-retention');
  });
});

describe("actions / GET /orgs/{org}/actions/permissions/fork-pr-contributor-approval", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/fork-pr-contributor-approval`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/permissions/fork-pr-contributor-approval');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /orgs/{org}/actions/permissions/fork-pr-contributor-approval", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/fork-pr-contributor-approval`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"approval_policy\":\"first_time_contributors_new_to_github\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/permissions/fork-pr-contributor-approval');
  });
});

describe("actions / GET /orgs/{org}/actions/permissions/fork-pr-workflows-private-repos", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/fork-pr-workflows-private-repos`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/permissions/fork-pr-workflows-private-repos');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /orgs/{org}/actions/permissions/fork-pr-workflows-private-repos", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/fork-pr-workflows-private-repos`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"run_workflows_from_fork_pull_requests\":true}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/permissions/fork-pr-workflows-private-repos');
  });
});

describe("actions / GET /orgs/{org}/actions/permissions/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/permissions/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /orgs/{org}/actions/permissions/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/repositories`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_repository_ids\":[1]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/permissions/repositories');
  });
});

describe("actions / PUT /orgs/{org}/actions/permissions/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/repositories/sample`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/permissions/repositories/{repository_id}');
  });
});

describe("actions / DELETE /orgs/{org}/actions/permissions/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/repositories/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/actions/permissions/repositories/{repository_id}');
  });
});

describe("actions / GET /orgs/{org}/actions/permissions/selected-actions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/selected-actions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/permissions/selected-actions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /orgs/{org}/actions/permissions/selected-actions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/selected-actions`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"github_owned_allowed\":true,\"verified_allowed\":true,\"patterns_allowed\":[\"sample\"]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/permissions/selected-actions');
  });
});

describe("actions / GET /orgs/{org}/actions/permissions/self-hosted-runners", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/self-hosted-runners`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/permissions/self-hosted-runners');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /orgs/{org}/actions/permissions/self-hosted-runners", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/self-hosted-runners`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"enabled_repositories\":\"all\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/permissions/self-hosted-runners');
  });
});

describe("actions / GET /orgs/{org}/actions/permissions/self-hosted-runners/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/self-hosted-runners/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/permissions/self-hosted-runners/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /orgs/{org}/actions/permissions/self-hosted-runners/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/self-hosted-runners/repositories`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_repository_ids\":[1]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/permissions/self-hosted-runners/repositories');
  });
});

describe("actions / PUT /orgs/{org}/actions/permissions/self-hosted-runners/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/self-hosted-runners/repositories/sample`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/permissions/self-hosted-runners/repositories/{repository_id}');
  });
});

describe("actions / DELETE /orgs/{org}/actions/permissions/self-hosted-runners/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/self-hosted-runners/repositories/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/actions/permissions/self-hosted-runners/repositories/{repository_id}');
  });
});

describe("actions / GET /orgs/{org}/actions/permissions/workflow", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/workflow`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/permissions/workflow');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /orgs/{org}/actions/permissions/workflow", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/permissions/workflow`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"default_workflow_permissions\":\"sample\",\"can_approve_pull_request_reviews\":\"sample\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/permissions/workflow');
  });
});

describe("actions / GET /orgs/{org}/actions/runner-groups", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runner-groups`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/runner-groups');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / POST /orgs/{org}/actions/runner-groups", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runner-groups`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/actions/runner-groups');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/runner-groups/{runner_group_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runner-groups/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/runner-groups/{runner_group_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runner-groups/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}');
  });
});

describe("actions / PATCH /orgs/{org}/actions/runner-groups/{runner_group_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runner-groups/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/actions/runner-groups/{runner_group_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/runner-groups/{runner_group_id}/hosted-runners", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runner-groups/sample/hosted-runners`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/runner-groups/{runner_group_id}/hosted-runners');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runner-groups/sample/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runner-groups/sample/repositories`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_repository_ids\":[1]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories');
  });
});

describe("actions / PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runner-groups/sample/repositories/sample`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories/{repository_id}');
  });
});

describe("actions / DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runner-groups/sample/repositories/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories/{repository_id}');
  });
});

describe("actions / GET /orgs/{org}/actions/runner-groups/{runner_group_id}/runners", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runner-groups/sample/runners`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/runner-groups/{runner_group_id}/runners');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/runners", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runner-groups/sample/runners`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"runners\":[1]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/runners');
  });
});

describe("actions / PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/runners/{runner_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runner-groups/sample/runners/sample`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/runners/{runner_id}');
  });
});

describe("actions / DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}/runners/{runner_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runner-groups/sample/runners/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}/runners/{runner_id}');
  });
});

describe("actions / GET /orgs/{org}/actions/runners", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runners`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/runners');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/runners/deprecations/{version}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runners/deprecations/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/runners/deprecations/{version}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/runners/downloads", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runners/downloads`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/runners/downloads');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / POST /orgs/{org}/actions/runners/generate-jitconfig", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runners/generate-jitconfig`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"runner_group_id\":1,\"labels\":[\"sample\"]}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/actions/runners/generate-jitconfig');
  });
});

describe("actions / POST /orgs/{org}/actions/runners/registration-token", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runners/registration-token`, {
      method: "POST",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/actions/runners/registration-token');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / POST /orgs/{org}/actions/runners/remove-token", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runners/remove-token`, {
      method: "POST",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/actions/runners/remove-token');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/runners/{runner_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runners/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/runners/{runner_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / DELETE /orgs/{org}/actions/runners/{runner_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runners/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/actions/runners/{runner_id}');
  });
});

describe("actions / GET /orgs/{org}/actions/runners/{runner_id}/labels", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runners/sample/labels`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/runners/{runner_id}/labels');
  });
});

describe("actions / PUT /orgs/{org}/actions/runners/{runner_id}/labels", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runners/sample/labels`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"labels\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/runners/{runner_id}/labels');
  });
});

describe("actions / POST /orgs/{org}/actions/runners/{runner_id}/labels", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runners/sample/labels`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"labels\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/actions/runners/{runner_id}/labels');
  });
});

describe("actions / DELETE /orgs/{org}/actions/runners/{runner_id}/labels", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runners/sample/labels`, {
      method: "DELETE",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/actions/runners/{runner_id}/labels');
  });
});

describe("actions / DELETE /orgs/{org}/actions/runners/{runner_id}/labels/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/runners/sample/labels/sample`, {
      method: "DELETE",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/actions/runners/{runner_id}/labels/{name}');
  });
});

describe("actions / GET /orgs/{org}/actions/secrets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/secrets`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/secrets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/secrets/public-key", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/secrets/public-key`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/secrets/public-key');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/secrets/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /orgs/{org}/actions/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/secrets/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"encrypted_value\":\"sample\",\"key_id\":\"sample\",\"visibility\":\"all\"}",
    });
    const okCodes = ["201","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / DELETE /orgs/{org}/actions/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/secrets/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/actions/secrets/{secret_name}');
  });
});

describe("actions / GET /orgs/{org}/actions/secrets/{secret_name}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/secrets/sample/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/secrets/{secret_name}/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /orgs/{org}/actions/secrets/{secret_name}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/secrets/sample/repositories`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_repository_ids\":[1]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/secrets/{secret_name}/repositories');
  });
});

describe("actions / PUT /orgs/{org}/actions/secrets/{secret_name}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/secrets/sample/repositories/1`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/secrets/{secret_name}/repositories/{repository_id}');
  });
});

describe("actions / DELETE /orgs/{org}/actions/secrets/{secret_name}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/secrets/sample/repositories/1`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/actions/secrets/{secret_name}/repositories/{repository_id}');
  });
});

describe("actions / GET /orgs/{org}/actions/variables", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/variables`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/variables');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / POST /orgs/{org}/actions/variables", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/variables`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"value\":\"sample\",\"visibility\":\"all\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/actions/variables');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /orgs/{org}/actions/variables/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/variables/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/variables/{name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / DELETE /orgs/{org}/actions/variables/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/variables/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/actions/variables/{name}');
  });
});

describe("actions / PATCH /orgs/{org}/actions/variables/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/variables/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"value\":\"sample\",\"visibility\":\"all\",\"selected_repository_ids\":[1]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/actions/variables/{name}');
  });
});

describe("actions / GET /orgs/{org}/actions/variables/{name}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/variables/sample/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/actions/variables/{name}/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /orgs/{org}/actions/variables/{name}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/variables/sample/repositories`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_repository_ids\":[1]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/variables/{name}/repositories');
  });
});

describe("actions / PUT /orgs/{org}/actions/variables/{name}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/variables/sample/repositories/1`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/actions/variables/{name}/repositories/{repository_id}');
  });
});

describe("actions / DELETE /orgs/{org}/actions/variables/{name}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/actions/variables/sample/repositories/1`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/actions/variables/{name}/repositories/{repository_id}');
  });
});

describe("agents / GET /orgs/{org}/agents/secrets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/secrets`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/agents/secrets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / GET /orgs/{org}/agents/secrets/public-key", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/secrets/public-key`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/agents/secrets/public-key');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / GET /orgs/{org}/agents/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/secrets/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/agents/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / PUT /orgs/{org}/agents/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/secrets/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"encrypted_value\":\"sample\",\"key_id\":\"sample\",\"visibility\":\"all\"}",
    });
    const okCodes = ["201","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/agents/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / DELETE /orgs/{org}/agents/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/secrets/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/agents/secrets/{secret_name}');
  });
});

describe("agents / GET /orgs/{org}/agents/secrets/{secret_name}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/secrets/sample/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/agents/secrets/{secret_name}/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / PUT /orgs/{org}/agents/secrets/{secret_name}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/secrets/sample/repositories`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_repository_ids\":[1]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/agents/secrets/{secret_name}/repositories');
  });
});

describe("agents / PUT /orgs/{org}/agents/secrets/{secret_name}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/secrets/sample/repositories/1`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/agents/secrets/{secret_name}/repositories/{repository_id}');
  });
});

describe("agents / DELETE /orgs/{org}/agents/secrets/{secret_name}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/secrets/sample/repositories/1`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/agents/secrets/{secret_name}/repositories/{repository_id}');
  });
});

describe("agents / GET /orgs/{org}/agents/variables", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/variables`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/agents/variables');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / POST /orgs/{org}/agents/variables", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/variables`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"value\":\"sample\",\"visibility\":\"all\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/agents/variables');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / GET /orgs/{org}/agents/variables/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/variables/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/agents/variables/{name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / DELETE /orgs/{org}/agents/variables/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/variables/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/agents/variables/{name}');
  });
});

describe("agents / PATCH /orgs/{org}/agents/variables/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/variables/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"value\":\"sample\",\"visibility\":\"all\",\"selected_repository_ids\":[1]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/agents/variables/{name}');
  });
});

describe("agents / GET /orgs/{org}/agents/variables/{name}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/variables/sample/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/agents/variables/{name}/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / PUT /orgs/{org}/agents/variables/{name}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/variables/sample/repositories`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_repository_ids\":[1]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/agents/variables/{name}/repositories');
  });
});

describe("agents / PUT /orgs/{org}/agents/variables/{name}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/variables/sample/repositories/1`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/agents/variables/{name}/repositories/{repository_id}');
  });
});

describe("agents / DELETE /orgs/{org}/agents/variables/{name}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/agents/variables/sample/repositories/1`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/agents/variables/{name}/repositories/{repository_id}');
  });
});

describe("orgs / POST /orgs/{org}/artifacts/metadata/deployment-record", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/artifacts/metadata/deployment-record`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"libfoo\",\"digest\":\"xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\",\"status\":\"deployed\",\"logical_environment\":\"sample\",\"deployment_name\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/artifacts/metadata/deployment-record');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / POST /orgs/{org}/artifacts/metadata/deployment-record/cluster/{cluster}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/artifacts/metadata/deployment-record/cluster/sample`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"logical_environment\":\"sample\",\"deployments\":[{\"name\":\"sample\",\"deployment_name\":\"sample\",\"digest\":\"xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\"}]}",
    });
    const okCodes = ["200","207"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/artifacts/metadata/deployment-record/cluster/{cluster}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / POST /orgs/{org}/artifacts/metadata/deployment-record/cluster/{cluster}/jobs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/artifacts/metadata/deployment-record/cluster/sample/jobs`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"logical_environment\":\"sample\",\"deployments\":[{\"name\":\"sample\",\"deployment_name\":\"sample\",\"digest\":\"xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\"}]}",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/artifacts/metadata/deployment-record/cluster/{cluster}/jobs');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/artifacts/metadata/deployment-record/cluster/{cluster}/jobs/{job_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/artifacts/metadata/deployment-record/cluster/sample/jobs/1`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/artifacts/metadata/deployment-record/cluster/{cluster}/jobs/{job_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / POST /orgs/{org}/artifacts/metadata/storage-record", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/artifacts/metadata/storage-record`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"libfoo\",\"digest\":\"sha256:0ecbaa601dba202129058746c7d8e3f282d0efb5fff0...\",\"registry_url\":\"https://reg.example.com/artifactory/\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/artifacts/metadata/storage-record');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/artifacts/{subject_digest}/metadata/deployment-records", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/artifacts/sample/metadata/deployment-records`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/artifacts/{subject_digest}/metadata/deployment-records');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/artifacts/{subject_digest}/metadata/storage-records", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/artifacts/xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx/metadata/storage-records`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/artifacts/{subject_digest}/metadata/storage-records');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / POST /orgs/{org}/attestations/bulk-list", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/attestations/bulk-list`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"subject_digests\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/attestations/bulk-list');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / POST /orgs/{org}/attestations/delete-request", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/attestations/delete-request`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"subject_digests\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/attestations/delete-request');
  });
});

describe("orgs / DELETE /orgs/{org}/attestations/digest/{subject_digest}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/attestations/digest/sample`, {
      method: "DELETE",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/attestations/digest/{subject_digest}');
  });
});

describe("orgs / GET /orgs/{org}/attestations/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/attestations/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/attestations/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / DELETE /orgs/{org}/attestations/{attestation_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/attestations/1`, {
      method: "DELETE",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/attestations/{attestation_id}');
  });
});

describe("orgs / GET /orgs/{org}/attestations/{subject_digest}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/attestations/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/attestations/{subject_digest}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/blocks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/blocks`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/blocks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/blocks/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/blocks/sample`, {
      method: "GET",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/blocks/{username}');
  });
});

describe("orgs / PUT /orgs/{org}/blocks/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/blocks/sample`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/blocks/{username}');
  });
});

describe("orgs / DELETE /orgs/{org}/blocks/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/blocks/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/blocks/{username}');
  });
});

describe("campaigns / GET /orgs/{org}/campaigns", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/campaigns`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/campaigns');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("campaigns / POST /orgs/{org}/campaigns", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/campaigns`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "\"sample\"",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/campaigns');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("campaigns / GET /orgs/{org}/campaigns/{campaign_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/campaigns/1`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/campaigns/{campaign_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("campaigns / DELETE /orgs/{org}/campaigns/{campaign_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/campaigns/1`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/campaigns/{campaign_number}');
  });
});

describe("campaigns / PATCH /orgs/{org}/campaigns/{campaign_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/campaigns/1`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"description\":\"sample\",\"managers\":[\"sample\"],\"team_managers\":[\"sample\"],\"ends_at\":\"2026-01-01T00:00:00Z\",\"contact_link\":\"https://example.com\",\"state\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/campaigns/{campaign_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / GET /orgs/{org}/code-scanning/ai-scan", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/code-scanning/ai-scan`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/code-scanning/ai-scan');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / PATCH /orgs/{org}/code-scanning/ai-scan", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/code-scanning/ai-scan`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"pr_scan\":\"enabled\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/code-scanning/ai-scan');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / GET /orgs/{org}/code-scanning/alerts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/code-scanning/alerts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/code-scanning/alerts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-security / GET /orgs/{org}/code-security/configurations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/code-security/configurations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/code-security/configurations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-security / POST /orgs/{org}/code-security/configurations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/code-security/configurations`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/code-security/configurations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-security / GET /orgs/{org}/code-security/configurations/defaults", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/code-security/configurations/defaults`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/code-security/configurations/defaults');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-security / DELETE /orgs/{org}/code-security/configurations/detach", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/code-security/configurations/detach`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_repository_ids\":[1]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/code-security/configurations/detach');
  });
});

describe("code-security / GET /orgs/{org}/code-security/configurations/{configuration_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/code-security/configurations/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/code-security/configurations/{configuration_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-security / DELETE /orgs/{org}/code-security/configurations/{configuration_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/code-security/configurations/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/code-security/configurations/{configuration_id}');
  });
});

describe("code-security / PATCH /orgs/{org}/code-security/configurations/{configuration_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/code-security/configurations/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"description\":\"sample\",\"advanced_security\":\"enabled\",\"code_security\":\"enabled\",\"dependency_graph\":\"enabled\",\"dependency_graph_autosubmit_action\":\"enabled\",\"dependency_graph_autosubmit_action_options\":{\"labeled_runners\":true},\"dependabot_alerts\":\"enabled\",\"dependabot_security_updates\":\"enabled\",\"dependabot_delegated_alert_dismissal\":\"enabled\",\"code_scanning_default_setup\":\"enabled\",\"code_scanning_default_setup_options\":\"sample\",\"code_scanning_options\":\"sample\",\"code_scanning_delegated_alert_dismissal\":\"disabled\",\"secret_protection\":\"enabled\",\"secret_scanning\":\"enabled\",\"secret_scanning_push_protection\":\"enabled\",\"secret_scanning_delegated_bypass\":\"enabled\",\"secret_scanning_delegated_bypass_options\":{\"reviewers\":[{\"reviewer_id\":1,\"reviewer_type\":\"TEAM\"}]},\"secret_scanning_validity_checks\":\"enabled\",\"secret_scanning_non_provider_patterns\":\"enabled\",\"secret_scanning_generic_secrets\":\"enabled\",\"secret_scanning_delegated_alert_dismissal\":\"enabled\",\"secret_scanning_extended_metadata\":\"enabled\",\"private_vulnerability_reporting\":\"enabled\",\"enforcement\":\"enforced\"}",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/code-security/configurations/{configuration_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-security / POST /orgs/{org}/code-security/configurations/{configuration_id}/attach", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/code-security/configurations/sample/attach`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"scope\":\"all\"}",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/code-security/configurations/{configuration_id}/attach');
  });
});

describe("code-security / PUT /orgs/{org}/code-security/configurations/{configuration_id}/defaults", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/code-security/configurations/sample/defaults`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"default_for_new_repos\":\"all\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/code-security/configurations/{configuration_id}/defaults');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-security / GET /orgs/{org}/code-security/configurations/{configuration_id}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/code-security/configurations/sample/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/code-security/configurations/{configuration_id}/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / GET /orgs/{org}/codespaces", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/codespaces`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/codespaces');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / PUT /orgs/{org}/codespaces/access", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/codespaces/access`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"visibility\":\"disabled\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/codespaces/access');
  });
});

describe("codespaces / POST /orgs/{org}/codespaces/access/selected_users", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/codespaces/access/selected_users`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_usernames\":[\"sample\"]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/codespaces/access/selected_users');
  });
});

describe("codespaces / DELETE /orgs/{org}/codespaces/access/selected_users", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/codespaces/access/selected_users`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_usernames\":[\"sample\"]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/codespaces/access/selected_users');
  });
});

describe("codespaces / GET /orgs/{org}/codespaces/secrets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/codespaces/secrets`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/codespaces/secrets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / GET /orgs/{org}/codespaces/secrets/public-key", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/codespaces/secrets/public-key`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/codespaces/secrets/public-key');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / GET /orgs/{org}/codespaces/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/codespaces/secrets/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/codespaces/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / PUT /orgs/{org}/codespaces/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/codespaces/secrets/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"visibility\":\"all\"}",
    });
    const okCodes = ["201","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/codespaces/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / DELETE /orgs/{org}/codespaces/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/codespaces/secrets/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/codespaces/secrets/{secret_name}');
  });
});

describe("codespaces / GET /orgs/{org}/codespaces/secrets/{secret_name}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/codespaces/secrets/sample/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/codespaces/secrets/{secret_name}/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / PUT /orgs/{org}/codespaces/secrets/{secret_name}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/codespaces/secrets/sample/repositories`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_repository_ids\":[1]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/codespaces/secrets/{secret_name}/repositories');
  });
});

describe("codespaces / PUT /orgs/{org}/codespaces/secrets/{secret_name}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/codespaces/secrets/sample/repositories/1`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/codespaces/secrets/{secret_name}/repositories/{repository_id}');
  });
});

describe("codespaces / DELETE /orgs/{org}/codespaces/secrets/{secret_name}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/codespaces/secrets/sample/repositories/1`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/codespaces/secrets/{secret_name}/repositories/{repository_id}');
  });
});

describe("copilot-spaces / GET /orgs/{org}/copilot-spaces", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot-spaces`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/copilot-spaces');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / POST /orgs/{org}/copilot-spaces", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot-spaces`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"Team Planning Space\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/copilot-spaces');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / GET /orgs/{org}/copilot-spaces/{space_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot-spaces/1`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/copilot-spaces/{space_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / PUT /orgs/{org}/copilot-spaces/{space_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot-spaces/1`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"Updated Team Planning Space\",\"description\":\"Updated organization space for team planning and coordination\",\"general_instructions\":\"Updated instructions to help the team with planning and coordination tasks\",\"base_role\":\"reader\",\"resources_attributes\":[{\"resource_type\":\"repository\",\"metadata\":{\"repository_id\":1,\"file_path\":\"sample\",\"text\":\"sample\",\"name\":\"sample\",\"number\":1}}]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/copilot-spaces/{space_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / DELETE /orgs/{org}/copilot-spaces/{space_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot-spaces/1`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/copilot-spaces/{space_number}');
  });
});

describe("copilot-spaces / GET /orgs/{org}/copilot-spaces/{space_number}/collaborators", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot-spaces/1/collaborators`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/copilot-spaces/{space_number}/collaborators');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / POST /orgs/{org}/copilot-spaces/{space_number}/collaborators", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot-spaces/1/collaborators`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"actor_type\":\"User\",\"actor_identifier\":\"sample\",\"role\":\"reader\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/copilot-spaces/{space_number}/collaborators');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / PUT /orgs/{org}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot-spaces/1/collaborators/User/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"role\":\"reader\"}",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / DELETE /orgs/{org}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot-spaces/1/collaborators/User/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}');
  });
});

describe("copilot-spaces / GET /orgs/{org}/copilot-spaces/{space_number}/resources", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot-spaces/1/resources`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/copilot-spaces/{space_number}/resources');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / POST /orgs/{org}/copilot-spaces/{space_number}/resources", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot-spaces/1/resources`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"resource_type\":\"repository\",\"metadata\":{}}",
    });
    const okCodes = ["200","201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/copilot-spaces/{space_number}/resources');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / GET /orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot-spaces/1/resources/1`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / PUT /orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot-spaces/1/resources/1`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"metadata\":{}}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / DELETE /orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot-spaces/1/resources/1`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/copilot-spaces/{space_number}/resources/{space_resource_id}');
  });
});

describe("copilot / GET /orgs/{org}/copilot/billing", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/billing`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/copilot/billing');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / GET /orgs/{org}/copilot/billing/seats", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/billing/seats`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/copilot/billing/seats');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / POST /orgs/{org}/copilot/billing/selected_teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/billing/selected_teams`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_teams\":[\"sample\"]}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/copilot/billing/selected_teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / DELETE /orgs/{org}/copilot/billing/selected_teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/billing/selected_teams`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_teams\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/copilot/billing/selected_teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / POST /orgs/{org}/copilot/billing/selected_users", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/billing/selected_users`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_usernames\":[\"sample\"]}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/copilot/billing/selected_users');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / DELETE /orgs/{org}/copilot/billing/selected_users", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/billing/selected_users`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_usernames\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/copilot/billing/selected_users');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / GET /orgs/{org}/copilot/coding-agent/permissions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/coding-agent/permissions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/copilot/coding-agent/permissions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / PUT /orgs/{org}/copilot/coding-agent/permissions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/coding-agent/permissions`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"enabled_repositories\":\"all\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/copilot/coding-agent/permissions');
  });
});

describe("copilot / GET /orgs/{org}/copilot/coding-agent/permissions/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/coding-agent/permissions/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/copilot/coding-agent/permissions/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / PUT /orgs/{org}/copilot/coding-agent/permissions/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/coding-agent/permissions/repositories`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_repository_ids\":[1]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/copilot/coding-agent/permissions/repositories');
  });
});

describe("copilot / PUT /orgs/{org}/copilot/coding-agent/permissions/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/coding-agent/permissions/repositories/sample`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/copilot/coding-agent/permissions/repositories/{repository_id}');
  });
});

describe("copilot / DELETE /orgs/{org}/copilot/coding-agent/permissions/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/coding-agent/permissions/repositories/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/copilot/coding-agent/permissions/repositories/{repository_id}');
  });
});

describe("copilot / GET /orgs/{org}/copilot/content_exclusion", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/content_exclusion`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/copilot/content_exclusion');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / PUT /orgs/{org}/copilot/content_exclusion", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/content_exclusion`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/copilot/content_exclusion');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / GET /orgs/{org}/copilot/metrics/reports/organization-1-day", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/metrics/reports/organization-1-day`, {
      method: "GET",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/copilot/metrics/reports/organization-1-day');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / GET /orgs/{org}/copilot/metrics/reports/organization-28-day/latest", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/metrics/reports/organization-28-day/latest`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/copilot/metrics/reports/organization-28-day/latest');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / GET /orgs/{org}/copilot/metrics/reports/repos-1-day", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/metrics/reports/repos-1-day`, {
      method: "GET",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/copilot/metrics/reports/repos-1-day');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / GET /orgs/{org}/copilot/metrics/reports/user-teams-1-day", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/metrics/reports/user-teams-1-day`, {
      method: "GET",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/copilot/metrics/reports/user-teams-1-day');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / GET /orgs/{org}/copilot/metrics/reports/users-1-day", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/metrics/reports/users-1-day`, {
      method: "GET",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/copilot/metrics/reports/users-1-day');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / GET /orgs/{org}/copilot/metrics/reports/users-28-day/latest", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/copilot/metrics/reports/users-28-day/latest`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/copilot/metrics/reports/users-28-day/latest');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / GET /orgs/{org}/dependabot/alerts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/dependabot/alerts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/dependabot/alerts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / GET /orgs/{org}/dependabot/repository-access", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/dependabot/repository-access`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/dependabot/repository-access');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / PATCH /orgs/{org}/dependabot/repository-access", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/dependabot/repository-access`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"repository_ids_to_add\":[123,456],\"repository_ids_to_remove\":[789]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/dependabot/repository-access');
  });
});

describe("dependabot / PUT /orgs/{org}/dependabot/repository-access/default-level", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/dependabot/repository-access/default-level`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"default_level\":\"internal\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/dependabot/repository-access/default-level');
  });
});

describe("dependabot / GET /orgs/{org}/dependabot/secrets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/dependabot/secrets`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/dependabot/secrets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / GET /orgs/{org}/dependabot/secrets/public-key", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/dependabot/secrets/public-key`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/dependabot/secrets/public-key');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / GET /orgs/{org}/dependabot/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/dependabot/secrets/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/dependabot/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / PUT /orgs/{org}/dependabot/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/dependabot/secrets/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"visibility\":\"all\"}",
    });
    const okCodes = ["201","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/dependabot/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / DELETE /orgs/{org}/dependabot/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/dependabot/secrets/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/dependabot/secrets/{secret_name}');
  });
});

describe("dependabot / GET /orgs/{org}/dependabot/secrets/{secret_name}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/dependabot/secrets/sample/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/dependabot/secrets/{secret_name}/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / PUT /orgs/{org}/dependabot/secrets/{secret_name}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/dependabot/secrets/sample/repositories`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_repository_ids\":[1]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/dependabot/secrets/{secret_name}/repositories');
  });
});

describe("dependabot / PUT /orgs/{org}/dependabot/secrets/{secret_name}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/dependabot/secrets/sample/repositories/1`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/dependabot/secrets/{secret_name}/repositories/{repository_id}');
  });
});

describe("dependabot / DELETE /orgs/{org}/dependabot/secrets/{secret_name}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/dependabot/secrets/sample/repositories/1`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/dependabot/secrets/{secret_name}/repositories/{repository_id}');
  });
});

describe("packages / GET /orgs/{org}/docker/conflicts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/docker/conflicts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/docker/conflicts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /orgs/{org}/events", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/events`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/events');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/failed_invitations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/failed_invitations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/failed_invitations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/hooks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/hooks`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/hooks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / POST /orgs/{org}/hooks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/hooks`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"config\":{\"url\":\"sample\"}}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/hooks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/hooks/{hook_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/hooks/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/hooks/{hook_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / DELETE /orgs/{org}/hooks/{hook_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/hooks/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/hooks/{hook_id}');
  });
});

describe("orgs / PATCH /orgs/{org}/hooks/{hook_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/hooks/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"config\":{\"url\":\"sample\"},\"events\":[\"push\"],\"active\":true,\"name\":\"\\\"web\\\"\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/hooks/{hook_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/hooks/{hook_id}/config", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/hooks/sample/config`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/hooks/{hook_id}/config');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / PATCH /orgs/{org}/hooks/{hook_id}/config", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/hooks/sample/config`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"url\":\"sample\",\"content_type\":\"sample\",\"secret\":\"sample\",\"insecure_ssl\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/hooks/{hook_id}/config');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/hooks/{hook_id}/deliveries", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/hooks/sample/deliveries`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/hooks/{hook_id}/deliveries');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/hooks/{hook_id}/deliveries/{delivery_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/hooks/sample/deliveries/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/hooks/{hook_id}/deliveries/{delivery_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / POST /orgs/{org}/hooks/{hook_id}/deliveries/{delivery_id}/attempts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/hooks/sample/deliveries/sample/attempts`, {
      method: "POST",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/hooks/{hook_id}/deliveries/{delivery_id}/attempts');
  });
});

describe("orgs / POST /orgs/{org}/hooks/{hook_id}/pings", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/hooks/sample/pings`, {
      method: "POST",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/hooks/{hook_id}/pings');
  });
});

describe("orgs / GET /orgs/{org}/insights/api/route-stats/{actor_type}/{actor_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/insights/api/route-stats/sample/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/insights/api/route-stats/{actor_type}/{actor_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/insights/api/subject-stats", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/insights/api/subject-stats`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/insights/api/subject-stats');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/insights/api/summary-stats", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/insights/api/summary-stats`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/insights/api/summary-stats');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/insights/api/summary-stats/users/{user_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/insights/api/summary-stats/users/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/insights/api/summary-stats/users/{user_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/insights/api/summary-stats/{actor_type}/{actor_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/insights/api/summary-stats/sample/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/insights/api/summary-stats/{actor_type}/{actor_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/insights/api/time-stats", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/insights/api/time-stats`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/insights/api/time-stats');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/insights/api/time-stats/users/{user_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/insights/api/time-stats/users/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/insights/api/time-stats/users/{user_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/insights/api/time-stats/{actor_type}/{actor_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/insights/api/time-stats/sample/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/insights/api/time-stats/{actor_type}/{actor_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/insights/api/user-stats/{user_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/insights/api/user-stats/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/insights/api/user-stats/{user_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /orgs/{org}/installation", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/installation`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/installation');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/installations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/installations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/installations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("interactions / GET /orgs/{org}/interaction-limits", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/interaction-limits`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/interaction-limits');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("interactions / PUT /orgs/{org}/interaction-limits", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/interaction-limits`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"limit\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/interaction-limits');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("interactions / DELETE /orgs/{org}/interaction-limits", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/interaction-limits`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/interaction-limits');
  });
});

describe("interactions / GET /orgs/{org}/interaction-limits/pulls/creation-cap", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/interaction-limits/pulls/creation-cap`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/interaction-limits/pulls/creation-cap');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("interactions / PATCH /orgs/{org}/interaction-limits/pulls/creation-cap", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/interaction-limits/pulls/creation-cap`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"enabled\":true}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/interaction-limits/pulls/creation-cap');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/invitations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/invitations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/invitations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / POST /orgs/{org}/invitations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/invitations`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"invitee_id\":1,\"email\":\"sample\",\"role\":\"direct_member\",\"team_ids\":[1]}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/invitations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / DELETE /orgs/{org}/invitations/{invitation_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/invitations/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/invitations/{invitation_id}');
  });
});

describe("orgs / GET /orgs/{org}/invitations/{invitation_id}/teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/invitations/sample/teams`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/invitations/{invitation_id}/teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/issue-fields", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/issue-fields`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/issue-fields');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / POST /orgs/{org}/issue-fields", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/issue-fields`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"data_type\":\"text\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/issue-fields');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / DELETE /orgs/{org}/issue-fields/{issue_field_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/issue-fields/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/issue-fields/{issue_field_id}');
  });
});

describe("orgs / PATCH /orgs/{org}/issue-fields/{issue_field_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/issue-fields/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"description\":\"sample\",\"visibility\":\"organization_members_only\",\"options\":[{\"name\":\"sample\",\"color\":\"gray\",\"priority\":1}]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/issue-fields/{issue_field_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/issue-types", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/issue-types`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/issue-types');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / POST /orgs/{org}/issue-types", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/issue-types`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"is_enabled\":true}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/issue-types');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / PUT /orgs/{org}/issue-types/{issue_type_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/issue-types/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"is_enabled\":true}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/issue-types/{issue_type_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / DELETE /orgs/{org}/issue-types/{issue_type_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/issue-types/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/issue-types/{issue_type_id}');
  });
});

describe("issues / GET /orgs/{org}/issues", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/issues`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/issues');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/members", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/members`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/members');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/members/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/members/sample`, {
      method: "GET",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/members/{username}');
  });
});

describe("orgs / DELETE /orgs/{org}/members/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/members/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/members/{username}');
  });
});

describe("codespaces / GET /orgs/{org}/members/{username}/codespaces", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/members/sample/codespaces`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/members/{username}/codespaces');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / DELETE /orgs/{org}/members/{username}/codespaces/{codespace_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/members/sample/codespaces/sample`, {
      method: "DELETE",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/members/{username}/codespaces/{codespace_name}');
  });
});

describe("codespaces / POST /orgs/{org}/members/{username}/codespaces/{codespace_name}/stop", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/members/sample/codespaces/sample/stop`, {
      method: "POST",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/members/{username}/codespaces/{codespace_name}/stop');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / GET /orgs/{org}/members/{username}/copilot", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/members/sample/copilot`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/members/{username}/copilot');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/memberships/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/memberships/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/memberships/{username}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / PUT /orgs/{org}/memberships/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/memberships/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"role\":\"member\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/memberships/{username}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / DELETE /orgs/{org}/memberships/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/memberships/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/memberships/{username}');
  });
});

describe("migrations / GET /orgs/{org}/migrations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/migrations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/migrations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("migrations / POST /orgs/{org}/migrations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/migrations`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"repositories\":[\"sample\"]}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/migrations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("migrations / GET /orgs/{org}/migrations/{migration_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/migrations/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/migrations/{migration_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("migrations / GET /orgs/{org}/migrations/{migration_id}/archive", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/migrations/sample/archive`, {
      method: "GET",
    });
    const okCodes = ["302"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/migrations/{migration_id}/archive');
  });
});

describe("migrations / DELETE /orgs/{org}/migrations/{migration_id}/archive", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/migrations/sample/archive`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/migrations/{migration_id}/archive');
  });
});

describe("migrations / DELETE /orgs/{org}/migrations/{migration_id}/repos/{repo_name}/lock", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/migrations/sample/repos/sample/lock`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/migrations/{migration_id}/repos/{repo_name}/lock');
  });
});

describe("migrations / GET /orgs/{org}/migrations/{migration_id}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/migrations/sample/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/migrations/{migration_id}/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/organization-roles", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/organization-roles`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/organization-roles');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / DELETE /orgs/{org}/organization-roles/teams/{team_slug}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/organization-roles/teams/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/organization-roles/teams/{team_slug}');
  });
});

describe("orgs / PUT /orgs/{org}/organization-roles/teams/{team_slug}/{role_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/organization-roles/teams/sample/sample`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/organization-roles/teams/{team_slug}/{role_id}');
  });
});

describe("orgs / DELETE /orgs/{org}/organization-roles/teams/{team_slug}/{role_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/organization-roles/teams/sample/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/organization-roles/teams/{team_slug}/{role_id}');
  });
});

describe("orgs / DELETE /orgs/{org}/organization-roles/users/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/organization-roles/users/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/organization-roles/users/{username}');
  });
});

describe("orgs / PUT /orgs/{org}/organization-roles/users/{username}/{role_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/organization-roles/users/sample/sample`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/organization-roles/users/{username}/{role_id}');
  });
});

describe("orgs / DELETE /orgs/{org}/organization-roles/users/{username}/{role_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/organization-roles/users/sample/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/organization-roles/users/{username}/{role_id}');
  });
});

describe("orgs / GET /orgs/{org}/organization-roles/{role_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/organization-roles/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/organization-roles/{role_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/organization-roles/{role_id}/teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/organization-roles/sample/teams`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/organization-roles/{role_id}/teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/organization-roles/{role_id}/users", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/organization-roles/sample/users`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/organization-roles/{role_id}/users');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/outside_collaborators", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/outside_collaborators`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/outside_collaborators');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / PUT /orgs/{org}/outside_collaborators/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/outside_collaborators/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"async\":false}",
    });
    const okCodes = ["202","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/outside_collaborators/{username}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / DELETE /orgs/{org}/outside_collaborators/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/outside_collaborators/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/outside_collaborators/{username}');
  });
});

describe("packages / GET /orgs/{org}/packages", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/packages?package_type=npm`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/packages');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("packages / GET /orgs/{org}/packages/{package_type}/{package_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/packages/sample/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/packages/{package_type}/{package_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("packages / DELETE /orgs/{org}/packages/{package_type}/{package_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/packages/sample/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/packages/{package_type}/{package_name}');
  });
});

describe("packages / POST /orgs/{org}/packages/{package_type}/{package_name}/restore", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/packages/sample/sample/restore`, {
      method: "POST",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/packages/{package_type}/{package_name}/restore');
  });
});

describe("packages / GET /orgs/{org}/packages/{package_type}/{package_name}/versions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/packages/sample/sample/versions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/packages/{package_type}/{package_name}/versions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("packages / GET /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/packages/sample/sample/versions/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("packages / DELETE /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/packages/sample/sample/versions/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}');
  });
});

describe("packages / POST /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}/restore", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/packages/sample/sample/versions/sample/restore`, {
      method: "POST",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}/restore');
  });
});

describe("orgs / GET /orgs/{org}/personal-access-token-requests", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/personal-access-token-requests`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/personal-access-token-requests');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / POST /orgs/{org}/personal-access-token-requests", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/personal-access-token-requests`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"action\":\"approve\"}",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/personal-access-token-requests');
  });
});

describe("orgs / POST /orgs/{org}/personal-access-token-requests/{pat_request_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/personal-access-token-requests/1`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"action\":\"approve\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/personal-access-token-requests/{pat_request_id}');
  });
});

describe("orgs / GET /orgs/{org}/personal-access-token-requests/{pat_request_id}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/personal-access-token-requests/1/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/personal-access-token-requests/{pat_request_id}/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/personal-access-tokens", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/personal-access-tokens`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/personal-access-tokens');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / POST /orgs/{org}/personal-access-tokens", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/personal-access-tokens`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"action\":\"revoke\",\"pat_ids\":[1]}",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/personal-access-tokens');
  });
});

describe("orgs / POST /orgs/{org}/personal-access-tokens/{pat_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/personal-access-tokens/sample`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"action\":\"revoke\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/personal-access-tokens/{pat_id}');
  });
});

describe("orgs / GET /orgs/{org}/personal-access-tokens/{pat_id}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/personal-access-tokens/1/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/personal-access-tokens/{pat_id}/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("private-registries / GET /orgs/{org}/private-registries", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/private-registries`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/private-registries');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("private-registries / POST /orgs/{org}/private-registries", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/private-registries`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"registry_type\":\"maven_repository\",\"url\":\"https://example.com\",\"visibility\":\"all\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/private-registries');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("private-registries / GET /orgs/{org}/private-registries/public-key", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/private-registries/public-key`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/private-registries/public-key');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("private-registries / GET /orgs/{org}/private-registries/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/private-registries/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/private-registries/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("private-registries / DELETE /orgs/{org}/private-registries/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/private-registries/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/private-registries/{secret_name}');
  });
});

describe("private-registries / PATCH /orgs/{org}/private-registries/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/private-registries/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"registry_type\":\"maven_repository\",\"url\":\"https://example.com\",\"username\":\"sample\",\"replaces_base\":false,\"encrypted_value\":\"sample\",\"key_id\":\"sample\",\"visibility\":\"all\",\"selected_repository_ids\":[1],\"auth_type\":\"token\",\"tenant_id\":\"sample\",\"client_id\":\"sample\",\"aws_region\":\"sample\",\"account_id\":\"sample\",\"role_name\":\"sample\",\"domain\":\"sample\",\"domain_owner\":\"sample\",\"jfrog_oidc_provider_name\":\"sample\",\"audience\":\"sample\",\"identity_mapping_name\":\"sample\",\"namespace\":\"sample\",\"service_slug\":\"sample\",\"api_host\":\"sample\",\"workload_identity_provider\":\"sample\",\"service_account\":\"sample\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/private-registries/{secret_name}');
  });
});

describe("projects / GET /orgs/{org}/projectsV2", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/projectsV2`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/projectsV2');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / GET /orgs/{org}/projectsV2/{project_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/projectsV2/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/projectsV2/{project_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / POST /orgs/{org}/projectsV2/{project_number}/drafts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/projectsV2/sample/drafts`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"title\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/projectsV2/{project_number}/drafts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / GET /orgs/{org}/projectsV2/{project_number}/fields", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/projectsV2/sample/fields`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/projectsV2/{project_number}/fields');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / POST /orgs/{org}/projectsV2/{project_number}/fields", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/projectsV2/sample/fields`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"issue_field_id\":1}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/projectsV2/{project_number}/fields');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / GET /orgs/{org}/projectsV2/{project_number}/fields/{field_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/projectsV2/sample/fields/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/projectsV2/{project_number}/fields/{field_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / GET /orgs/{org}/projectsV2/{project_number}/items", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/projectsV2/sample/items`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/projectsV2/{project_number}/items');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / POST /orgs/{org}/projectsV2/{project_number}/items", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/projectsV2/sample/items`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "\"sample\"",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/projectsV2/{project_number}/items');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / GET /orgs/{org}/projectsV2/{project_number}/items/{item_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/projectsV2/sample/items/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/projectsV2/{project_number}/items/{item_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / DELETE /orgs/{org}/projectsV2/{project_number}/items/{item_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/projectsV2/sample/items/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/projectsV2/{project_number}/items/{item_id}');
  });
});

describe("projects / PATCH /orgs/{org}/projectsV2/{project_number}/items/{item_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/projectsV2/sample/items/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"fields\":[{\"id\":1,\"value\":\"sample\"}]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/projectsV2/{project_number}/items/{item_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / POST /orgs/{org}/projectsV2/{project_number}/views", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/projectsV2/sample/views`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"Sprint Board\",\"layout\":\"board\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/projectsV2/{project_number}/views');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / GET /orgs/{org}/projectsV2/{project_number}/views/{view_number}/items", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/projectsV2/sample/views/sample/items`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/projectsV2/{project_number}/views/{view_number}/items');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/properties/schema", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/properties/schema`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/properties/schema');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / PATCH /orgs/{org}/properties/schema", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/properties/schema`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"properties\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/properties/schema');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/properties/schema/{custom_property_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/properties/schema/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/properties/schema/{custom_property_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / PUT /orgs/{org}/properties/schema/{custom_property_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/properties/schema/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"value_type\":\"single_select\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/properties/schema/{custom_property_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / DELETE /orgs/{org}/properties/schema/{custom_property_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/properties/schema/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/properties/schema/{custom_property_name}');
  });
});

describe("orgs / GET /orgs/{org}/properties/values", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/properties/values`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/properties/values');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / PATCH /orgs/{org}/properties/values", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/properties/values`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"repository_names\":[\"sample\"],\"properties\":[\"sample\"]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/properties/values');
  });
});

describe("orgs / GET /orgs/{org}/public_members", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/public_members`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/public_members');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/public_members/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/public_members/sample`, {
      method: "GET",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/public_members/{username}');
  });
});

describe("orgs / PUT /orgs/{org}/public_members/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/public_members/sample`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/public_members/{username}');
  });
});

describe("orgs / DELETE /orgs/{org}/public_members/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/public_members/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/public_members/{username}');
  });
});

describe("repos / GET /orgs/{org}/repos", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/repos`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/repos');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /orgs/{org}/repos", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/repos`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/repos');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /orgs/{org}/rulesets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/rulesets`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/rulesets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /orgs/{org}/rulesets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/rulesets`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"enforcement\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/rulesets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /orgs/{org}/rulesets/rule-suites", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/rulesets/rule-suites`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/rulesets/rule-suites');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /orgs/{org}/rulesets/rule-suites/{rule_suite_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/rulesets/rule-suites/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/rulesets/rule-suites/{rule_suite_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /orgs/{org}/rulesets/{ruleset_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/rulesets/1`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/rulesets/{ruleset_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / PUT /orgs/{org}/rulesets/{ruleset_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/rulesets/1`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"target\":\"branch\",\"enforcement\":\"sample\",\"bypass_actors\":[\"sample\"],\"conditions\":\"sample\",\"rules\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/rulesets/{ruleset_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /orgs/{org}/rulesets/{ruleset_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/rulesets/1`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/rulesets/{ruleset_id}');
  });
});

describe("orgs / GET /orgs/{org}/rulesets/{ruleset_id}/history", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/rulesets/1/history`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/rulesets/{ruleset_id}/history');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/rulesets/{ruleset_id}/history/{version_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/rulesets/1/history/1`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/rulesets/{ruleset_id}/history/{version_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("secret-scanning / GET /orgs/{org}/secret-scanning/alerts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/secret-scanning/alerts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/secret-scanning/alerts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("secret-scanning / GET /orgs/{org}/secret-scanning/custom-patterns", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/secret-scanning/custom-patterns`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/secret-scanning/custom-patterns');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("secret-scanning / POST /orgs/{org}/secret-scanning/custom-patterns", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/secret-scanning/custom-patterns`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"patterns\":[\"sample\"]}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/secret-scanning/custom-patterns');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("secret-scanning / DELETE /orgs/{org}/secret-scanning/custom-patterns", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/secret-scanning/custom-patterns`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"patterns\":[\"sample\"]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/secret-scanning/custom-patterns');
  });
});

describe("secret-scanning / PATCH /orgs/{org}/secret-scanning/custom-patterns/{pattern_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/secret-scanning/custom-patterns/1`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "\"sample\"",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/secret-scanning/custom-patterns/{pattern_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("secret-scanning / GET /orgs/{org}/secret-scanning/pattern-configurations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/secret-scanning/pattern-configurations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/secret-scanning/pattern-configurations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("secret-scanning / PATCH /orgs/{org}/secret-scanning/pattern-configurations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/secret-scanning/pattern-configurations`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"pattern_config_version\":\"sample\",\"provider_pattern_settings\":[{\"token_type\":\"sample\",\"push_protection_setting\":\"not-set\"}],\"custom_pattern_settings\":[{\"token_type\":\"sample\",\"custom_pattern_version\":\"sample\",\"push_protection_setting\":\"disabled\"}]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/secret-scanning/pattern-configurations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("security-advisories / GET /orgs/{org}/security-advisories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/security-advisories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/security-advisories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /orgs/{org}/security-managers", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/security-managers`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/security-managers');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / PUT /orgs/{org}/security-managers/teams/{team_slug}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/security-managers/teams/sample`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/security-managers/teams/{team_slug}');
  });
});

describe("orgs / DELETE /orgs/{org}/security-managers/teams/{team_slug}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/security-managers/teams/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/security-managers/teams/{team_slug}');
  });
});

describe("orgs / GET /orgs/{org}/settings/immutable-releases", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/settings/immutable-releases`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/settings/immutable-releases');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / PUT /orgs/{org}/settings/immutable-releases", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/settings/immutable-releases`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"enforced_repositories\":\"all\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/settings/immutable-releases');
  });
});

describe("orgs / GET /orgs/{org}/settings/immutable-releases/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/settings/immutable-releases/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/settings/immutable-releases/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / PUT /orgs/{org}/settings/immutable-releases/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/settings/immutable-releases/repositories`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_repository_ids\":[1]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/settings/immutable-releases/repositories');
  });
});

describe("orgs / PUT /orgs/{org}/settings/immutable-releases/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/settings/immutable-releases/repositories/sample`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/settings/immutable-releases/repositories/{repository_id}');
  });
});

describe("orgs / DELETE /orgs/{org}/settings/immutable-releases/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/settings/immutable-releases/repositories/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/settings/immutable-releases/repositories/{repository_id}');
  });
});

describe("hosted-compute / GET /orgs/{org}/settings/network-configurations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/settings/network-configurations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/settings/network-configurations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("hosted-compute / POST /orgs/{org}/settings/network-configurations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/settings/network-configurations`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"network_settings_ids\":[\"sample\"]}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/settings/network-configurations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("hosted-compute / GET /orgs/{org}/settings/network-configurations/{network_configuration_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/settings/network-configurations/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/settings/network-configurations/{network_configuration_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("hosted-compute / DELETE /orgs/{org}/settings/network-configurations/{network_configuration_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/settings/network-configurations/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/settings/network-configurations/{network_configuration_id}');
  });
});

describe("hosted-compute / PATCH /orgs/{org}/settings/network-configurations/{network_configuration_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/settings/network-configurations/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"compute_service\":\"none\",\"network_settings_ids\":[\"sample\"],\"failover_network_settings_ids\":[\"sample\"],\"failover_network_enabled\":true}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/settings/network-configurations/{network_configuration_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("hosted-compute / GET /orgs/{org}/settings/network-settings/{network_settings_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/settings/network-settings/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/settings/network-settings/{network_settings_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / GET /orgs/{org}/teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/teams`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / POST /orgs/{org}/teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/teams`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / GET /orgs/{org}/teams/{team_slug}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/teams/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/teams/{team_slug}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / DELETE /orgs/{org}/teams/{team_slug}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/teams/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/teams/{team_slug}');
  });
});

describe("teams / PATCH /orgs/{org}/teams/{team_slug}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/teams/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"description\":\"sample\",\"privacy\":\"secret\",\"notification_setting\":\"notifications_enabled\",\"permission\":\"pull\",\"parent_team_id\":1,\"parent_team_slug\":\"sample\"}",
    });
    const okCodes = ["200","201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /orgs/{org}/teams/{team_slug}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / GET /orgs/{org}/teams/{team_slug}/invitations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/teams/sample/invitations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/teams/{team_slug}/invitations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / GET /orgs/{org}/teams/{team_slug}/members", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/teams/sample/members`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/teams/{team_slug}/members');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / GET /orgs/{org}/teams/{team_slug}/memberships/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/teams/sample/memberships/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/teams/{team_slug}/memberships/{username}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / PUT /orgs/{org}/teams/{team_slug}/memberships/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/teams/sample/memberships/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"role\":\"member\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/teams/{team_slug}/memberships/{username}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / DELETE /orgs/{org}/teams/{team_slug}/memberships/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/teams/sample/memberships/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/teams/{team_slug}/memberships/{username}');
  });
});

describe("teams / GET /orgs/{org}/teams/{team_slug}/repos", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/teams/sample/repos`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/teams/{team_slug}/repos');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / GET /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/teams/sample/repos/sample/sample`, {
      method: "GET",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / PUT /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/teams/sample/repos/sample/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"permission\":\"sample\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}');
  });
});

describe("teams / DELETE /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/teams/sample/repos/sample/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}');
  });
});

describe("teams / GET /orgs/{org}/teams/{team_slug}/teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/teams/sample/teams`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /orgs/{org}/teams/{team_slug}/teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / POST /orgs/{org}/{security_product}/{enablement}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/orgs/sample/sample/sample`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"query_suite\":\"default\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /orgs/{org}/{security_product}/{enablement}');
  });
});

describe("rate-limit / GET /rate_limit", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/rate_limit`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /rate_limit');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}');
  });
});

describe("repos / PATCH /repos/{owner}/{repo}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"description\":\"sample\",\"homepage\":\"sample\",\"private\":false,\"visibility\":\"public\",\"security_and_analysis\":{\"advanced_security\":{\"status\":\"sample\"},\"code_security\":{\"status\":\"sample\"},\"secret_scanning\":{\"status\":\"sample\"},\"secret_scanning_push_protection\":{\"status\":\"sample\"},\"secret_scanning_ai_detection\":{\"status\":\"sample\"},\"secret_scanning_non_provider_patterns\":{\"status\":\"sample\"},\"secret_scanning_delegated_alert_dismissal\":{\"status\":\"sample\"},\"secret_scanning_delegated_bypass\":{\"status\":\"sample\"},\"secret_scanning_delegated_bypass_options\":{\"reviewers\":[{\"reviewer_id\":1,\"reviewer_type\":\"TEAM\"}]}},\"has_issues\":true,\"has_projects\":true,\"has_wiki\":true,\"has_pull_requests\":true,\"pull_request_creation_policy\":\"all\",\"is_template\":false,\"default_branch\":\"sample\",\"allow_squash_merge\":true,\"allow_merge_commit\":true,\"allow_rebase_merge\":true,\"allow_auto_merge\":false,\"delete_branch_on_merge\":false,\"allow_update_branch\":false,\"use_squash_pr_title_as_default\":false,\"squash_merge_commit_title\":\"PR_TITLE\",\"squash_merge_commit_message\":\"PR_BODY\",\"merge_commit_title\":\"PR_TITLE\",\"merge_commit_message\":\"PR_BODY\",\"archived\":false,\"allow_forking\":false,\"web_commit_signoff_required\":false}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/artifacts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/artifacts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/artifacts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/artifacts/{artifact_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/artifacts/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/artifacts/{artifact_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / DELETE /repos/{owner}/{repo}/actions/artifacts/{artifact_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/artifacts/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/actions/artifacts/{artifact_id}');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/artifacts/{artifact_id}/{archive_format}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/artifacts/sample/sample`, {
      method: "GET",
    });
    const okCodes = ["302"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/artifacts/{artifact_id}/{archive_format}');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/cache/retention-limit", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/cache/retention-limit`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/cache/retention-limit');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /repos/{owner}/{repo}/actions/cache/retention-limit", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/cache/retention-limit`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"max_cache_retention_days\":14}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/actions/cache/retention-limit');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/cache/storage-limit", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/cache/storage-limit`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/cache/storage-limit');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /repos/{owner}/{repo}/actions/cache/storage-limit", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/cache/storage-limit`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"max_cache_size_gb\":10}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/actions/cache/storage-limit');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/cache/usage", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/cache/usage`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/cache/usage');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/caches", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/caches`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/caches');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / DELETE /repos/{owner}/{repo}/actions/caches", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/caches`, {
      method: "DELETE",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/actions/caches');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / DELETE /repos/{owner}/{repo}/actions/caches/{cache_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/caches/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/actions/caches/{cache_id}');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/concurrency_groups", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/concurrency_groups`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/concurrency_groups');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/concurrency_groups/{concurrency_group_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/concurrency_groups/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/concurrency_groups/{concurrency_group_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/jobs/{job_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/jobs/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/jobs/{job_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/jobs/{job_id}/logs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/jobs/sample/logs`, {
      method: "GET",
    });
    const okCodes = ["302"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/jobs/{job_id}/logs');
  });
});

describe("actions / POST /repos/{owner}/{repo}/actions/jobs/{job_id}/rerun", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/jobs/sample/rerun`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"enable_debug_logging\":false,\"enable_debugger\":false}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/actions/jobs/{job_id}/rerun');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/oidc/customization/sub", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/oidc/customization/sub`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/oidc/customization/sub');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /repos/{owner}/{repo}/actions/oidc/customization/sub", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/oidc/customization/sub`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"use_default\":true}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/actions/oidc/customization/sub');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/organization-secrets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/organization-secrets`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/organization-secrets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/organization-variables", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/organization-variables`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/organization-variables');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/permissions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/permissions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/permissions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /repos/{owner}/{repo}/actions/permissions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/permissions`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"enabled\":\"sample\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/actions/permissions');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/permissions/access", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/permissions/access`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/permissions/access');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /repos/{owner}/{repo}/actions/permissions/access", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/permissions/access`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"access_level\":\"none\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/actions/permissions/access');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/permissions/artifact-and-log-retention", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/permissions/artifact-and-log-retention`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/permissions/artifact-and-log-retention');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /repos/{owner}/{repo}/actions/permissions/artifact-and-log-retention", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/permissions/artifact-and-log-retention`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"days\":1}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/actions/permissions/artifact-and-log-retention');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/permissions/fork-pr-contributor-approval", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/permissions/fork-pr-contributor-approval`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/permissions/fork-pr-contributor-approval');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /repos/{owner}/{repo}/actions/permissions/fork-pr-contributor-approval", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/permissions/fork-pr-contributor-approval`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"approval_policy\":\"first_time_contributors_new_to_github\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/actions/permissions/fork-pr-contributor-approval');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/permissions/fork-pr-workflows-private-repos", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/permissions/fork-pr-workflows-private-repos`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/permissions/fork-pr-workflows-private-repos');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /repos/{owner}/{repo}/actions/permissions/fork-pr-workflows-private-repos", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/permissions/fork-pr-workflows-private-repos`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"run_workflows_from_fork_pull_requests\":true}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/actions/permissions/fork-pr-workflows-private-repos');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/permissions/selected-actions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/permissions/selected-actions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/permissions/selected-actions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /repos/{owner}/{repo}/actions/permissions/selected-actions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/permissions/selected-actions`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"github_owned_allowed\":true,\"verified_allowed\":true,\"patterns_allowed\":[\"sample\"]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/actions/permissions/selected-actions');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/permissions/workflow", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/permissions/workflow`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/permissions/workflow');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /repos/{owner}/{repo}/actions/permissions/workflow", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/permissions/workflow`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"default_workflow_permissions\":\"sample\",\"can_approve_pull_request_reviews\":\"sample\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/actions/permissions/workflow');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runners", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runners`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runners');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runners/deprecations/{version}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runners/deprecations/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runners/deprecations/{version}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runners/downloads", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runners/downloads`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runners/downloads');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / POST /repos/{owner}/{repo}/actions/runners/generate-jitconfig", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runners/generate-jitconfig`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"runner_group_id\":1,\"labels\":[\"sample\"]}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/actions/runners/generate-jitconfig');
  });
});

describe("actions / POST /repos/{owner}/{repo}/actions/runners/registration-token", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runners/registration-token`, {
      method: "POST",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/actions/runners/registration-token');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / POST /repos/{owner}/{repo}/actions/runners/remove-token", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runners/remove-token`, {
      method: "POST",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/actions/runners/remove-token');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runners/{runner_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runners/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runners/{runner_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / DELETE /repos/{owner}/{repo}/actions/runners/{runner_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runners/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/actions/runners/{runner_id}');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runners/{runner_id}/labels", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runners/sample/labels`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runners/{runner_id}/labels');
  });
});

describe("actions / PUT /repos/{owner}/{repo}/actions/runners/{runner_id}/labels", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runners/sample/labels`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"labels\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/actions/runners/{runner_id}/labels');
  });
});

describe("actions / POST /repos/{owner}/{repo}/actions/runners/{runner_id}/labels", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runners/sample/labels`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"labels\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/actions/runners/{runner_id}/labels');
  });
});

describe("actions / DELETE /repos/{owner}/{repo}/actions/runners/{runner_id}/labels", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runners/sample/labels`, {
      method: "DELETE",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/actions/runners/{runner_id}/labels');
  });
});

describe("actions / DELETE /repos/{owner}/{repo}/actions/runners/{runner_id}/labels/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runners/sample/labels/sample`, {
      method: "DELETE",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/actions/runners/{runner_id}/labels/{name}');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runs');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runs/{run_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runs/{run_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / DELETE /repos/{owner}/{repo}/actions/runs/{run_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/actions/runs/{run_id}');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runs/{run_id}/approvals", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/approvals`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runs/{run_id}/approvals');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / POST /repos/{owner}/{repo}/actions/runs/{run_id}/approve", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/approve`, {
      method: "POST",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/actions/runs/{run_id}/approve');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runs/{run_id}/artifacts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/artifacts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runs/{run_id}/artifacts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/attempts/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number}/jobs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/attempts/sample/jobs`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number}/jobs');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number}/logs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/attempts/sample/logs`, {
      method: "GET",
    });
    const okCodes = ["302"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runs/{run_id}/attempts/{attempt_number}/logs');
  });
});

describe("actions / POST /repos/{owner}/{repo}/actions/runs/{run_id}/cancel", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/cancel`, {
      method: "POST",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/actions/runs/{run_id}/cancel');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runs/{run_id}/concurrency_groups", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/concurrency_groups`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runs/{run_id}/concurrency_groups');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / POST /repos/{owner}/{repo}/actions/runs/{run_id}/deployment_protection_rule", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/deployment_protection_rule`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "\"sample\"",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/actions/runs/{run_id}/deployment_protection_rule');
  });
});

describe("actions / POST /repos/{owner}/{repo}/actions/runs/{run_id}/force-cancel", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/force-cancel`, {
      method: "POST",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/actions/runs/{run_id}/force-cancel');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runs/{run_id}/jobs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/jobs`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runs/{run_id}/jobs');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runs/{run_id}/logs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/logs`, {
      method: "GET",
    });
    const okCodes = ["302"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runs/{run_id}/logs');
  });
});

describe("actions / DELETE /repos/{owner}/{repo}/actions/runs/{run_id}/logs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/logs`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/actions/runs/{run_id}/logs');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runs/{run_id}/pending_deployments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/pending_deployments`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runs/{run_id}/pending_deployments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / POST /repos/{owner}/{repo}/actions/runs/{run_id}/pending_deployments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/pending_deployments`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"environment_ids\":[161171787,161171795],\"state\":\"approved\",\"comment\":\"Ship it!\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/actions/runs/{run_id}/pending_deployments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / POST /repos/{owner}/{repo}/actions/runs/{run_id}/rerun", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/rerun`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"enable_debug_logging\":false}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/actions/runs/{run_id}/rerun');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / POST /repos/{owner}/{repo}/actions/runs/{run_id}/rerun-failed-jobs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/rerun-failed-jobs`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"enable_debug_logging\":false}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/actions/runs/{run_id}/rerun-failed-jobs');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/runs/{run_id}/timing", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/runs/sample/timing`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/runs/{run_id}/timing');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/secrets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/secrets`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/secrets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/secrets/public-key", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/secrets/public-key`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/secrets/public-key');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/secrets/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /repos/{owner}/{repo}/actions/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/secrets/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"encrypted_value\":\"sample\",\"key_id\":\"sample\"}",
    });
    const okCodes = ["201","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/actions/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / DELETE /repos/{owner}/{repo}/actions/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/secrets/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/actions/secrets/{secret_name}');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/variables", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/variables`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/variables');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / POST /repos/{owner}/{repo}/actions/variables", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/variables`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"value\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/actions/variables');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/variables/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/variables/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/variables/{name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / DELETE /repos/{owner}/{repo}/actions/variables/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/variables/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/actions/variables/{name}');
  });
});

describe("actions / PATCH /repos/{owner}/{repo}/actions/variables/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/variables/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"value\":\"sample\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/actions/variables/{name}');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/workflows", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/workflows`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/workflows');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/workflows/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /repos/{owner}/{repo}/actions/workflows/{workflow_id}/disable", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/workflows/sample/disable`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/actions/workflows/{workflow_id}/disable');
  });
});

describe("actions / POST /repos/{owner}/{repo}/actions/workflows/{workflow_id}/dispatches", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/workflows/sample/dispatches`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"ref\":\"sample\"}",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/actions/workflows/{workflow_id}/dispatches');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /repos/{owner}/{repo}/actions/workflows/{workflow_id}/enable", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/workflows/sample/enable`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/actions/workflows/{workflow_id}/enable');
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}/runs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/workflows/sample/runs`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}/runs');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}/timing", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/actions/workflows/sample/timing`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}/timing');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/activity", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/activity`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/activity');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / GET /repos/{owner}/{repo}/agents/organization-secrets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/agents/organization-secrets`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/agents/organization-secrets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / GET /repos/{owner}/{repo}/agents/organization-variables", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/agents/organization-variables`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/agents/organization-variables');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / GET /repos/{owner}/{repo}/agents/secrets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/agents/secrets`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/agents/secrets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / GET /repos/{owner}/{repo}/agents/secrets/public-key", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/agents/secrets/public-key`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/agents/secrets/public-key');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / GET /repos/{owner}/{repo}/agents/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/agents/secrets/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/agents/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / PUT /repos/{owner}/{repo}/agents/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/agents/secrets/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"encrypted_value\":\"sample\",\"key_id\":\"sample\"}",
    });
    const okCodes = ["201","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/agents/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / DELETE /repos/{owner}/{repo}/agents/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/agents/secrets/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/agents/secrets/{secret_name}');
  });
});

describe("agents / GET /repos/{owner}/{repo}/agents/variables", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/agents/variables`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/agents/variables');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / POST /repos/{owner}/{repo}/agents/variables", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/agents/variables`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"value\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/agents/variables');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / GET /repos/{owner}/{repo}/agents/variables/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/agents/variables/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/agents/variables/{name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("agents / DELETE /repos/{owner}/{repo}/agents/variables/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/agents/variables/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/agents/variables/{name}');
  });
});

describe("agents / PATCH /repos/{owner}/{repo}/agents/variables/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/agents/variables/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"value\":\"sample\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/agents/variables/{name}');
  });
});

describe("issues / GET /repos/{owner}/{repo}/assignees", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/assignees`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/assignees');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/assignees/{assignee}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/assignees/sample`, {
      method: "GET",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/assignees/{assignee}');
  });
});

describe("repos / POST /repos/{owner}/{repo}/attestations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/attestations`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"bundle\":{\"mediaType\":\"sample\",\"verificationMaterial\":{},\"dsseEnvelope\":{}}}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/attestations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/attestations/{subject_digest}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/attestations/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/attestations/{subject_digest}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/autolinks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/autolinks`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/autolinks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/autolinks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/autolinks`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"key_prefix\":\"sample\",\"url_template\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/autolinks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/autolinks/{autolink_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/autolinks/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/autolinks/{autolink_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/autolinks/{autolink_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/autolinks/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/autolinks/{autolink_id}');
  });
});

describe("repos / GET /repos/{owner}/{repo}/automated-security-fixes", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/automated-security-fixes`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/automated-security-fixes');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / PUT /repos/{owner}/{repo}/automated-security-fixes", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/automated-security-fixes`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/automated-security-fixes');
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/automated-security-fixes", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/automated-security-fixes`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/automated-security-fixes');
  });
});

describe("repos / GET /repos/{owner}/{repo}/branches", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/branches');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/branches/{branch}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/branches/{branch}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/branches/{branch}/protection", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/branches/{branch}/protection');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / PUT /repos/{owner}/{repo}/branches/{branch}/protection", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"required_status_checks\":{\"strict\":true,\"contexts\":[\"sample\"]},\"enforce_admins\":true,\"required_pull_request_reviews\":{\"dismissal_restrictions\":{\"users\":[\"sample\"],\"teams\":[\"sample\"],\"apps\":[\"sample\"]},\"dismiss_stale_reviews\":true,\"require_code_owner_reviews\":true,\"required_approving_review_count\":1,\"require_last_push_approval\":false,\"bypass_pull_request_allowances\":{\"users\":[\"sample\"],\"teams\":[\"sample\"],\"apps\":[\"sample\"]}},\"restrictions\":{\"users\":[\"sample\"],\"teams\":[\"sample\"]}}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/branches/{branch}/protection');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/branches/{branch}/protection", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/branches/{branch}/protection');
  });
});

describe("repos / GET /repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/enforce_admins`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/enforce_admins`, {
      method: "POST",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/enforce_admins`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins');
  });
});

describe("repos / GET /repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/required_pull_request_reviews`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/required_pull_request_reviews`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews');
  });
});

describe("repos / PATCH /repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/required_pull_request_reviews`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"dismissal_restrictions\":{\"users\":[\"sample\"],\"teams\":[\"sample\"],\"apps\":[\"sample\"]},\"dismiss_stale_reviews\":true,\"require_code_owner_reviews\":true,\"required_approving_review_count\":1,\"require_last_push_approval\":false,\"bypass_pull_request_allowances\":{\"users\":[\"sample\"],\"teams\":[\"sample\"],\"apps\":[\"sample\"]}}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/branches/{branch}/protection/required_signatures", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/required_signatures`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/branches/{branch}/protection/required_signatures');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/branches/{branch}/protection/required_signatures", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/required_signatures`, {
      method: "POST",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/branches/{branch}/protection/required_signatures');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_signatures", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/required_signatures`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_signatures');
  });
});

describe("repos / GET /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/required_status_checks`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/required_status_checks`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks');
  });
});

describe("repos / PATCH /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/required_status_checks`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"strict\":true,\"contexts\":[\"sample\"],\"checks\":[{\"context\":\"sample\"}]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/required_status_checks/contexts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / PUT /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/required_status_checks/contexts`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"contexts\":[\"contexts\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/required_status_checks/contexts`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"contexts\":[\"contexts\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/required_status_checks/contexts`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"contexts\":[\"contexts\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/restrictions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/restrictions`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions');
  });
});

describe("repos / GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/restrictions/apps`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / PUT /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/restrictions/apps`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"apps\":[\"my-app\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/restrictions/apps`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"apps\":[\"my-app\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/restrictions/apps`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"apps\":[\"my-app\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/restrictions/teams`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / PUT /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/restrictions/teams`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"teams\":[\"justice-league\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/restrictions/teams`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"teams\":[\"my-team\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/restrictions/teams`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"teams\":[\"my-team\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/restrictions/users`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / PUT /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/restrictions/users`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"users\":[\"mona\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/restrictions/users`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"users\":[\"mona\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/protection/restrictions/users`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"users\":[\"mona\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/branches/{branch}/rename", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/branches/sample/rename`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"new_name\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/branches/{branch}/rename');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("checks / POST /repos/{owner}/{repo}/check-runs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/check-runs`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"status\":\"completed\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/check-runs');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("checks / GET /repos/{owner}/{repo}/check-runs/{check_run_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/check-runs/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/check-runs/{check_run_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("checks / PATCH /repos/{owner}/{repo}/check-runs/{check_run_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/check-runs/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/check-runs/{check_run_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("checks / GET /repos/{owner}/{repo}/check-runs/{check_run_id}/annotations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/check-runs/sample/annotations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/check-runs/{check_run_id}/annotations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("checks / POST /repos/{owner}/{repo}/check-runs/{check_run_id}/rerequest", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/check-runs/sample/rerequest`, {
      method: "POST",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/check-runs/{check_run_id}/rerequest');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("checks / POST /repos/{owner}/{repo}/check-suites", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/check-suites`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"head_sha\":\"sample\"}",
    });
    const okCodes = ["200","201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/check-suites');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("checks / PATCH /repos/{owner}/{repo}/check-suites/preferences", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/check-suites/preferences`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"auto_trigger_checks\":[{\"app_id\":1,\"setting\":true}]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/check-suites/preferences');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("checks / GET /repos/{owner}/{repo}/check-suites/{check_suite_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/check-suites/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/check-suites/{check_suite_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("checks / GET /repos/{owner}/{repo}/check-suites/{check_suite_id}/check-runs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/check-suites/sample/check-runs`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/check-suites/{check_suite_id}/check-runs');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("checks / POST /repos/{owner}/{repo}/check-suites/{check_suite_id}/rerequest", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/check-suites/sample/rerequest`, {
      method: "POST",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/check-suites/{check_suite_id}/rerequest');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-quality / GET /repos/{owner}/{repo}/code-quality/findings", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-quality/findings`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-quality/findings');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-quality / GET /repos/{owner}/{repo}/code-quality/findings/{finding_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-quality/findings/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-quality/findings/{finding_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-quality / GET /repos/{owner}/{repo}/code-quality/setup", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-quality/setup`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-quality/setup');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-quality / PATCH /repos/{owner}/{repo}/code-quality/setup", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-quality/setup`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "\"sample\"",
    });
    const okCodes = ["200","202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/code-quality/setup');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / GET /repos/{owner}/{repo}/code-scanning/ai-scan", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/ai-scan`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-scanning/ai-scan');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / PATCH /repos/{owner}/{repo}/code-scanning/ai-scan", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/ai-scan`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"pr_scan\":\"enabled\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/code-scanning/ai-scan');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / GET /repos/{owner}/{repo}/code-scanning/alerts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/alerts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-scanning/alerts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / GET /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/alerts/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / PATCH /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/alerts/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "\"sample\"",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / GET /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/autofix", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/alerts/sample/autofix`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/autofix');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / POST /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/autofix", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/alerts/sample/autofix`, {
      method: "POST",
    });
    const okCodes = ["200","202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/autofix');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / POST /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/autofix/commits", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/alerts/sample/autofix/commits`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"target_ref\":\"sample\",\"message\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/autofix/commits');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / GET /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/instances", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/alerts/sample/instances`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/instances');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / GET /repos/{owner}/{repo}/code-scanning/analyses", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/analyses`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-scanning/analyses');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / GET /repos/{owner}/{repo}/code-scanning/analyses/{analysis_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/analyses/1`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-scanning/analyses/{analysis_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / DELETE /repos/{owner}/{repo}/code-scanning/analyses/{analysis_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/analyses/1`, {
      method: "DELETE",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/code-scanning/analyses/{analysis_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / GET /repos/{owner}/{repo}/code-scanning/codeql/databases", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/codeql/databases`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-scanning/codeql/databases');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / GET /repos/{owner}/{repo}/code-scanning/codeql/databases/{language}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/codeql/databases/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-scanning/codeql/databases/{language}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / DELETE /repos/{owner}/{repo}/code-scanning/codeql/databases/{language}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/codeql/databases/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/code-scanning/codeql/databases/{language}');
  });
});

describe("code-scanning / POST /repos/{owner}/{repo}/code-scanning/codeql/variant-analyses", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/codeql/variant-analyses`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "\"sample\"",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/code-scanning/codeql/variant-analyses');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / GET /repos/{owner}/{repo}/code-scanning/codeql/variant-analyses/{codeql_variant_analysis_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/codeql/variant-analyses/1`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-scanning/codeql/variant-analyses/{codeql_variant_analysis_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / GET /repos/{owner}/{repo}/code-scanning/codeql/variant-analyses/{codeql_variant_analysis_id}/repos/{repo_owner}/{repo_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/codeql/variant-analyses/1/repos/sample/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-scanning/codeql/variant-analyses/{codeql_variant_analysis_id}/repos/{repo_owner}/{repo_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / GET /repos/{owner}/{repo}/code-scanning/default-setup", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/default-setup`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-scanning/default-setup');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / PATCH /repos/{owner}/{repo}/code-scanning/default-setup", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/default-setup`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"state\":\"configured\",\"runner_type\":\"standard\",\"runner_label\":\"code-scanning\",\"query_suite\":\"default\",\"threat_model\":\"remote\",\"languages\":[\"actions\"]}",
    });
    const okCodes = ["200","202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/code-scanning/default-setup');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / POST /repos/{owner}/{repo}/code-scanning/sarifs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/sarifs`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"commit_sha\":\"sample\",\"ref\":\"sample\",\"sarif\":\"sample\"}",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/code-scanning/sarifs');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-scanning / GET /repos/{owner}/{repo}/code-scanning/sarifs/{sarif_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-scanning/sarifs/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-scanning/sarifs/{sarif_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("code-security / GET /repos/{owner}/{repo}/code-security-configuration", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/code-security-configuration`, {
      method: "GET",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/code-security-configuration');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/codeowners/errors", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/codeowners/errors`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/codeowners/errors');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / GET /repos/{owner}/{repo}/codespaces", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/codespaces`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/codespaces');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / POST /repos/{owner}/{repo}/codespaces", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/codespaces`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"ref\":\"sample\",\"location\":\"sample\",\"geo\":\"EuropeWest\",\"client_ip\":\"sample\",\"machine\":\"sample\",\"devcontainer_path\":\"sample\",\"multi_repo_permissions_opt_out\":true,\"working_directory\":\"sample\",\"idle_timeout_minutes\":1,\"display_name\":\"sample\",\"retention_period_minutes\":1}",
    });
    const okCodes = ["201","202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/codespaces');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / GET /repos/{owner}/{repo}/codespaces/devcontainers", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/codespaces/devcontainers`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/codespaces/devcontainers');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / GET /repos/{owner}/{repo}/codespaces/machines", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/codespaces/machines`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/codespaces/machines');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / GET /repos/{owner}/{repo}/codespaces/new", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/codespaces/new`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/codespaces/new');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / GET /repos/{owner}/{repo}/codespaces/permissions_check", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/codespaces/permissions_check?ref=master&devcontainer_path=.devcontainer%2Fexample%2Fdevcontainer.json`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/codespaces/permissions_check');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / GET /repos/{owner}/{repo}/codespaces/secrets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/codespaces/secrets`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/codespaces/secrets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / GET /repos/{owner}/{repo}/codespaces/secrets/public-key", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/codespaces/secrets/public-key`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/codespaces/secrets/public-key');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / GET /repos/{owner}/{repo}/codespaces/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/codespaces/secrets/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/codespaces/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / PUT /repos/{owner}/{repo}/codespaces/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/codespaces/secrets/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"encrypted_value\":\"sample\",\"key_id\":\"sample\"}",
    });
    const okCodes = ["201","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/codespaces/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / DELETE /repos/{owner}/{repo}/codespaces/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/codespaces/secrets/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/codespaces/secrets/{secret_name}');
  });
});

describe("repos / GET /repos/{owner}/{repo}/collaborators", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/collaborators`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/collaborators');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/collaborators/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/collaborators/sample`, {
      method: "GET",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/collaborators/{username}');
  });
});

describe("repos / PUT /repos/{owner}/{repo}/collaborators/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/collaborators/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"permission\":\"push\"}",
    });
    const okCodes = ["201","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/collaborators/{username}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/collaborators/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/collaborators/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/collaborators/{username}');
  });
});

describe("repos / GET /repos/{owner}/{repo}/collaborators/{username}/permission", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/collaborators/sample/permission`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/collaborators/{username}/permission');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/comments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/comments`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/comments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/comments/{comment_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/comments/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/comments/{comment_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/comments/{comment_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/comments/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/comments/{comment_id}');
  });
});

describe("repos / PATCH /repos/{owner}/{repo}/comments/{comment_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/comments/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"body\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/comments/{comment_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("reactions / GET /repos/{owner}/{repo}/comments/{comment_id}/reactions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/comments/sample/reactions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/comments/{comment_id}/reactions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("reactions / POST /repos/{owner}/{repo}/comments/{comment_id}/reactions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/comments/sample/reactions`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"content\":\"+1\"}",
    });
    const okCodes = ["200","201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/comments/{comment_id}/reactions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("reactions / DELETE /repos/{owner}/{repo}/comments/{comment_id}/reactions/{reaction_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/comments/sample/reactions/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/comments/{comment_id}/reactions/{reaction_id}');
  });
});

describe("repos / GET /repos/{owner}/{repo}/commits", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/commits`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/commits');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/commits/{commit_sha}/branches-where-head", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/commits/sample/branches-where-head`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/commits/{commit_sha}/branches-where-head');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/commits/{commit_sha}/comments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/commits/sample/comments`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/commits/{commit_sha}/comments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/commits/{commit_sha}/comments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/commits/sample/comments`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"body\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/commits/{commit_sha}/comments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/commits/{commit_sha}/pulls", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/commits/sample/pulls`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/commits/{commit_sha}/pulls');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/commits/{ref}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/commits/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/commits/{ref}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("checks / GET /repos/{owner}/{repo}/commits/{ref}/check-runs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/commits/sample/check-runs`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/commits/{ref}/check-runs');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("checks / GET /repos/{owner}/{repo}/commits/{ref}/check-suites", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/commits/sample/check-suites`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/commits/{ref}/check-suites');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/commits/{ref}/status", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/commits/sample/status`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/commits/{ref}/status');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/commits/{ref}/statuses", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/commits/sample/statuses`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/commits/{ref}/statuses');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/community/profile", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/community/profile`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/community/profile');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/compare/{basehead}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/compare/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/compare/{basehead}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/contents/{path}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/contents/sample`, {
      method: "GET",
      headers: { 'content-type': "application/json" },
      body: "\"sample\"",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/contents/{path}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/vnd.github.object, got ' + ct.split(';')[0]);
  });
});

describe("repos / PUT /repos/{owner}/{repo}/contents/{path}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/contents/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"message\":\"sample\",\"content\":\"sample\"}",
    });
    const okCodes = ["200","201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/contents/{path}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/contents/{path}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/contents/sample`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"message\":\"sample\",\"sha\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/contents/{path}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/contributors", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/contributors`, {
      method: "GET",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/contributors');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot / GET /repos/{owner}/{repo}/copilot/cloud-agent/configuration", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/copilot/cloud-agent/configuration`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/copilot/cloud-agent/configuration');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / GET /repos/{owner}/{repo}/dependabot/alerts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/dependabot/alerts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/dependabot/alerts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / GET /repos/{owner}/{repo}/dependabot/alerts/{alert_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/dependabot/alerts/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/dependabot/alerts/{alert_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / PATCH /repos/{owner}/{repo}/dependabot/alerts/{alert_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/dependabot/alerts/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "\"sample\"",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/dependabot/alerts/{alert_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / GET /repos/{owner}/{repo}/dependabot/secrets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/dependabot/secrets`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/dependabot/secrets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / GET /repos/{owner}/{repo}/dependabot/secrets/public-key", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/dependabot/secrets/public-key`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/dependabot/secrets/public-key');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / GET /repos/{owner}/{repo}/dependabot/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/dependabot/secrets/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/dependabot/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / PUT /repos/{owner}/{repo}/dependabot/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/dependabot/secrets/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"encrypted_value\":\"sample\",\"key_id\":\"sample\"}",
    });
    const okCodes = ["201","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/dependabot/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependabot / DELETE /repos/{owner}/{repo}/dependabot/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/dependabot/secrets/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/dependabot/secrets/{secret_name}');
  });
});

describe("dependency-graph / GET /repos/{owner}/{repo}/dependency-graph/compare/{basehead}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/dependency-graph/compare/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/dependency-graph/compare/{basehead}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependency-graph / GET /repos/{owner}/{repo}/dependency-graph/sbom", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/dependency-graph/sbom`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/dependency-graph/sbom');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependency-graph / GET /repos/{owner}/{repo}/dependency-graph/sbom/fetch-report/{sbom_uuid}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/dependency-graph/sbom/fetch-report/sample`, {
      method: "GET",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/dependency-graph/sbom/fetch-report/{sbom_uuid}');
  });
});

describe("dependency-graph / GET /repos/{owner}/{repo}/dependency-graph/sbom/generate-report", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/dependency-graph/sbom/generate-report`, {
      method: "GET",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/dependency-graph/sbom/generate-report');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("dependency-graph / POST /repos/{owner}/{repo}/dependency-graph/snapshots", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/dependency-graph/snapshots`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"detector\":{\"name\":\"docker buildtime detector\",\"version\":\"1.0.0\",\"url\":\"http://example.com/docker-buildtimer-detector\"},\"version\":1,\"ref\":\"refs/heads/main\",\"sha\":\"ddc951f4b1293222421f2c8df679786153acf689\",\"job\":{\"id\":\"5622a2b0-63f6-4732-8c34-a1ab27e102a11\",\"correlator\":\"yourworkflowname_yourjobname\"},\"scanned\":\"2020-06-13T14:52:50-05:00\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/dependency-graph/snapshots');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/deployments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/deployments`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/deployments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/deployments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/deployments`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"ref\":\"sample\"}",
    });
    const okCodes = ["201","202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/deployments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/deployments/{deployment_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/deployments/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/deployments/{deployment_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/deployments/{deployment_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/deployments/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/deployments/{deployment_id}');
  });
});

describe("repos / GET /repos/{owner}/{repo}/deployments/{deployment_id}/statuses", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/deployments/sample/statuses`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/deployments/{deployment_id}/statuses');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/deployments/{deployment_id}/statuses", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/deployments/sample/statuses`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"state\":\"error\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/deployments/{deployment_id}/statuses');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/deployments/{deployment_id}/statuses/{status_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/deployments/sample/statuses/1`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/deployments/{deployment_id}/statuses/{status_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/dispatches", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/dispatches`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"event_type\":\"sample\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/dispatches');
  });
});

describe("repos / GET /repos/{owner}/{repo}/environments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/environments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/environments/{environment_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/environments/{environment_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / PUT /repos/{owner}/{repo}/environments/{environment_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"wait_timer\":\"sample\",\"prevent_self_review\":\"sample\",\"reviewers\":[{\"type\":\"sample\",\"id\":4532992}],\"deployment_branch_policy\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/environments/{environment_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/environments/{environment_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/environments/{environment_name}');
  });
});

describe("repos / GET /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/deployment-branch-policies`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/deployment-branch-policies`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"release/*\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/deployment-branch-policies/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / PUT /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/deployment-branch-policies/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"release/*\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/deployment-branch-policies/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/environments/{environment_name}/deployment-branch-policies/{branch_policy_id}');
  });
});

describe("repos / GET /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/deployment_protection_rules`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/deployment_protection_rules`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"integration_id\":1}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules/apps", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/deployment_protection_rules/apps`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules/apps');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules/{protection_rule_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/deployment_protection_rules/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules/{protection_rule_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules/{protection_rule_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/deployment_protection_rules/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/environments/{environment_name}/deployment_protection_rules/{protection_rule_id}');
  });
});

describe("actions / GET /repos/{owner}/{repo}/environments/{environment_name}/secrets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/secrets`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/environments/{environment_name}/secrets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/environments/{environment_name}/secrets/public-key", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/secrets/public-key`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/environments/{environment_name}/secrets/public-key');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/environments/{environment_name}/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/secrets/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/environments/{environment_name}/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / PUT /repos/{owner}/{repo}/environments/{environment_name}/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/secrets/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"encrypted_value\":\"sample\",\"key_id\":\"sample\"}",
    });
    const okCodes = ["201","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/environments/{environment_name}/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / DELETE /repos/{owner}/{repo}/environments/{environment_name}/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/secrets/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/environments/{environment_name}/secrets/{secret_name}');
  });
});

describe("actions / GET /repos/{owner}/{repo}/environments/{environment_name}/variables", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/variables`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/environments/{environment_name}/variables');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / POST /repos/{owner}/{repo}/environments/{environment_name}/variables", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/variables`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"value\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/environments/{environment_name}/variables');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / GET /repos/{owner}/{repo}/environments/{environment_name}/variables/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/variables/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/environments/{environment_name}/variables/{name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("actions / DELETE /repos/{owner}/{repo}/environments/{environment_name}/variables/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/variables/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/environments/{environment_name}/variables/{name}');
  });
});

describe("actions / PATCH /repos/{owner}/{repo}/environments/{environment_name}/variables/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/environments/sample/variables/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"value\":\"sample\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/environments/{environment_name}/variables/{name}');
  });
});

describe("activity / GET /repos/{owner}/{repo}/events", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/events`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/events');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/forks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/forks`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/forks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/forks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/forks`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"organization\":\"sample\",\"name\":\"sample\",\"default_branch_only\":true}",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/forks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("git / POST /repos/{owner}/{repo}/git/blobs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/git/blobs`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"content\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/git/blobs');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("git / GET /repos/{owner}/{repo}/git/blobs/{file_sha}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/git/blobs/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/git/blobs/{file_sha}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("git / POST /repos/{owner}/{repo}/git/commits", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/git/commits`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"message\":\"sample\",\"tree\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/git/commits');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("git / GET /repos/{owner}/{repo}/git/commits/{commit_sha}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/git/commits/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/git/commits/{commit_sha}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("git / GET /repos/{owner}/{repo}/git/matching-refs/{ref}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/git/matching-refs/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/git/matching-refs/{ref}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("git / GET /repos/{owner}/{repo}/git/ref/{ref}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/git/ref/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/git/ref/{ref}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("git / POST /repos/{owner}/{repo}/git/refs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/git/refs`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"ref\":\"sample\",\"sha\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/git/refs');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("git / DELETE /repos/{owner}/{repo}/git/refs/{ref}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/git/refs/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/git/refs/{ref}');
  });
});

describe("git / PATCH /repos/{owner}/{repo}/git/refs/{ref}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/git/refs/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"sha\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/git/refs/{ref}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("git / POST /repos/{owner}/{repo}/git/tags", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/git/tags`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"tag\":\"sample\",\"message\":\"sample\",\"object\":\"sample\",\"type\":\"commit\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/git/tags');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("git / GET /repos/{owner}/{repo}/git/tags/{tag_sha}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/git/tags/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/git/tags/{tag_sha}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("git / POST /repos/{owner}/{repo}/git/trees", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/git/trees`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"tree\":[{\"path\":\"sample\",\"mode\":\"100644\",\"type\":\"blob\",\"sha\":\"sample\",\"content\":\"sample\"}]}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/git/trees');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("git / GET /repos/{owner}/{repo}/git/trees/{tree_sha}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/git/trees/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/git/trees/{tree_sha}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/hash-algorithm", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/hash-algorithm`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/hash-algorithm');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/hooks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/hooks`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/hooks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/hooks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/hooks`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"config\":{\"url\":\"sample\",\"content_type\":\"sample\",\"secret\":\"sample\",\"insecure_ssl\":\"sample\"},\"events\":[\"push\"],\"active\":true}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/hooks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/hooks/{hook_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/hooks/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/hooks/{hook_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/hooks/{hook_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/hooks/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/hooks/{hook_id}');
  });
});

describe("repos / PATCH /repos/{owner}/{repo}/hooks/{hook_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/hooks/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"config\":\"sample\",\"events\":[\"push\"],\"add_events\":[\"sample\"],\"remove_events\":[\"sample\"],\"active\":true}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/hooks/{hook_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/hooks/{hook_id}/config", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/hooks/sample/config`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/hooks/{hook_id}/config');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / PATCH /repos/{owner}/{repo}/hooks/{hook_id}/config", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/hooks/sample/config`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"url\":\"sample\",\"content_type\":\"sample\",\"secret\":\"sample\",\"insecure_ssl\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/hooks/{hook_id}/config');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/hooks/{hook_id}/deliveries", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/hooks/sample/deliveries`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/hooks/{hook_id}/deliveries');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/hooks/{hook_id}/deliveries/{delivery_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/hooks/sample/deliveries/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/hooks/{hook_id}/deliveries/{delivery_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/hooks/{hook_id}/deliveries/{delivery_id}/attempts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/hooks/sample/deliveries/sample/attempts`, {
      method: "POST",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/hooks/{hook_id}/deliveries/{delivery_id}/attempts');
  });
});

describe("repos / POST /repos/{owner}/{repo}/hooks/{hook_id}/pings", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/hooks/sample/pings`, {
      method: "POST",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/hooks/{hook_id}/pings');
  });
});

describe("repos / POST /repos/{owner}/{repo}/hooks/{hook_id}/tests", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/hooks/sample/tests`, {
      method: "POST",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/hooks/{hook_id}/tests');
  });
});

describe("repos / GET /repos/{owner}/{repo}/immutable-releases", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/immutable-releases`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/immutable-releases');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / PUT /repos/{owner}/{repo}/immutable-releases", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/immutable-releases`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/immutable-releases');
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/immutable-releases", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/immutable-releases`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/immutable-releases');
  });
});

describe("migrations / GET /repos/{owner}/{repo}/import", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/import`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/import');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("migrations / PUT /repos/{owner}/{repo}/import", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/import`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"vcs_url\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/import');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("migrations / DELETE /repos/{owner}/{repo}/import", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/import`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/import');
  });
});

describe("migrations / PATCH /repos/{owner}/{repo}/import", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/import`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"vcs_username\":\"sample\",\"vcs_password\":\"sample\",\"vcs\":\"\\\"git\\\"\",\"tfvc_project\":\"\\\"project1\\\"\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/import');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("migrations / GET /repos/{owner}/{repo}/import/authors", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/import/authors`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/import/authors');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("migrations / PATCH /repos/{owner}/{repo}/import/authors/{author_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/import/authors/1`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"email\":\"sample\",\"name\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/import/authors/{author_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("migrations / GET /repos/{owner}/{repo}/import/large_files", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/import/large_files`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/import/large_files');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("migrations / PATCH /repos/{owner}/{repo}/import/lfs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/import/lfs`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"use_lfs\":\"opt_in\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/import/lfs');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /repos/{owner}/{repo}/installation", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/installation`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/installation');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("interactions / GET /repos/{owner}/{repo}/interaction-limits", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/interaction-limits`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/interaction-limits');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("interactions / PUT /repos/{owner}/{repo}/interaction-limits", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/interaction-limits`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"limit\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/interaction-limits');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("interactions / DELETE /repos/{owner}/{repo}/interaction-limits", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/interaction-limits`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/interaction-limits');
  });
});

describe("interactions / GET /repos/{owner}/{repo}/interaction-limits/pulls/bypass-list", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/interaction-limits/pulls/bypass-list`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/interaction-limits/pulls/bypass-list');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("interactions / PUT /repos/{owner}/{repo}/interaction-limits/pulls/bypass-list", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/interaction-limits/pulls/bypass-list`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"users\":[\"octocat\",\"monalisa\"]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/interaction-limits/pulls/bypass-list');
  });
});

describe("interactions / DELETE /repos/{owner}/{repo}/interaction-limits/pulls/bypass-list", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/interaction-limits/pulls/bypass-list`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"users\":[\"octocat\",\"monalisa\"]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/interaction-limits/pulls/bypass-list');
  });
});

describe("interactions / GET /repos/{owner}/{repo}/interaction-limits/pulls/creation-cap", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/interaction-limits/pulls/creation-cap`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/interaction-limits/pulls/creation-cap');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("interactions / PATCH /repos/{owner}/{repo}/interaction-limits/pulls/creation-cap", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/interaction-limits/pulls/creation-cap`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"enabled\":true}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/interaction-limits/pulls/creation-cap');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/invitations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/invitations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/invitations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/invitations/{invitation_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/invitations/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/invitations/{invitation_id}');
  });
});

describe("repos / PATCH /repos/{owner}/{repo}/invitations/{invitation_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/invitations/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"permissions\":\"read\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/invitations/{invitation_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/issue-types", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issue-types`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issue-types');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / POST /repos/{owner}/{repo}/issues", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"title\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/issues');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues/comments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/comments`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/comments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues/comments/{comment_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/comments/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/comments/{comment_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / DELETE /repos/{owner}/{repo}/issues/comments/{comment_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/comments/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/issues/comments/{comment_id}');
  });
});

describe("issues / PATCH /repos/{owner}/{repo}/issues/comments/{comment_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/comments/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"body\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/issues/comments/{comment_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / PUT /repos/{owner}/{repo}/issues/comments/{comment_id}/pin", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/comments/sample/pin`, {
      method: "PUT",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/issues/comments/{comment_id}/pin');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / DELETE /repos/{owner}/{repo}/issues/comments/{comment_id}/pin", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/comments/sample/pin`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/issues/comments/{comment_id}/pin');
  });
});

describe("reactions / GET /repos/{owner}/{repo}/issues/comments/{comment_id}/reactions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/comments/sample/reactions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/comments/{comment_id}/reactions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("reactions / POST /repos/{owner}/{repo}/issues/comments/{comment_id}/reactions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/comments/sample/reactions`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"content\":\"+1\"}",
    });
    const okCodes = ["200","201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/issues/comments/{comment_id}/reactions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("reactions / DELETE /repos/{owner}/{repo}/issues/comments/{comment_id}/reactions/{reaction_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/comments/sample/reactions/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/issues/comments/{comment_id}/reactions/{reaction_id}');
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues/events", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/events`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/events');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues/events/{event_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/events/1`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/events/{event_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues/{issue_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/{issue_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / PATCH /repos/{owner}/{repo}/issues/{issue_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"title\":\"sample\",\"body\":\"sample\",\"assignee\":\"sample\",\"state\":\"open\",\"state_reason\":\"not_planned\",\"duplicate_issue_id\":1,\"milestone\":\"sample\",\"labels\":[\"sample\"],\"assignees\":[\"sample\"],\"issue_field_values\":[{\"field_id\":1,\"value\":\"sample\"}],\"type\":\"Epic\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/issues/{issue_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / POST /repos/{owner}/{repo}/issues/{issue_number}/assignees", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/assignees`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"assignees\":[\"sample\"]}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/issues/{issue_number}/assignees');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / DELETE /repos/{owner}/{repo}/issues/{issue_number}/assignees", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/assignees`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"assignees\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/issues/{issue_number}/assignees');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues/{issue_number}/assignees/{assignee}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/assignees/sample`, {
      method: "GET",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/{issue_number}/assignees/{assignee}');
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues/{issue_number}/comments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/comments`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/{issue_number}/comments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / POST /repos/{owner}/{repo}/issues/{issue_number}/comments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/comments`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"body\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/issues/{issue_number}/comments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocked_by", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/dependencies/blocked_by`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocked_by');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / POST /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocked_by", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/dependencies/blocked_by`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"issue_id\":1}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocked_by');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / DELETE /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocked_by/{issue_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/dependencies/blocked_by/1`, {
      method: "DELETE",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocked_by/{issue_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocking", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/dependencies/blocking`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/{issue_number}/dependencies/blocking');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues/{issue_number}/events", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/events`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/{issue_number}/events');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues/{issue_number}/issue-field-values", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/issue-field-values`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/{issue_number}/issue-field-values');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / PUT /repos/{owner}/{repo}/issues/{issue_number}/issue-field-values", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/issue-field-values`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"issue_field_values\":[{\"field_id\":123,\"value\":\"Critical\"}]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/issues/{issue_number}/issue-field-values');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / POST /repos/{owner}/{repo}/issues/{issue_number}/issue-field-values", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/issue-field-values`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"issue_field_values\":[{\"field_id\":123,\"value\":\"Critical\"}]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/issues/{issue_number}/issue-field-values');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / DELETE /repos/{owner}/{repo}/issues/{issue_number}/issue-field-values/{issue_field_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/issue-field-values/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/issues/{issue_number}/issue-field-values/{issue_field_id}');
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues/{issue_number}/labels", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/labels`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/{issue_number}/labels');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / PUT /repos/{owner}/{repo}/issues/{issue_number}/labels", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/labels`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"labels\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/issues/{issue_number}/labels');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / POST /repos/{owner}/{repo}/issues/{issue_number}/labels", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/labels`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"labels\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/issues/{issue_number}/labels');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / DELETE /repos/{owner}/{repo}/issues/{issue_number}/labels", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/labels`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/issues/{issue_number}/labels');
  });
});

describe("issues / DELETE /repos/{owner}/{repo}/issues/{issue_number}/labels/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/labels/sample`, {
      method: "DELETE",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/issues/{issue_number}/labels/{name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / PUT /repos/{owner}/{repo}/issues/{issue_number}/lock", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/lock`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"lock_reason\":\"off-topic\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/issues/{issue_number}/lock');
  });
});

describe("issues / DELETE /repos/{owner}/{repo}/issues/{issue_number}/lock", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/lock`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/issues/{issue_number}/lock');
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues/{issue_number}/parent", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/parent`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/{issue_number}/parent');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("reactions / GET /repos/{owner}/{repo}/issues/{issue_number}/reactions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/reactions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/{issue_number}/reactions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("reactions / POST /repos/{owner}/{repo}/issues/{issue_number}/reactions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/reactions`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"content\":\"+1\"}",
    });
    const okCodes = ["200","201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/issues/{issue_number}/reactions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("reactions / DELETE /repos/{owner}/{repo}/issues/{issue_number}/reactions/{reaction_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/reactions/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/issues/{issue_number}/reactions/{reaction_id}');
  });
});

describe("issues / DELETE /repos/{owner}/{repo}/issues/{issue_number}/sub_issue", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/sub_issue`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"sub_issue_id\":1}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/issues/{issue_number}/sub_issue');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues/{issue_number}/sub_issues", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/sub_issues`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/{issue_number}/sub_issues');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / POST /repos/{owner}/{repo}/issues/{issue_number}/sub_issues", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/sub_issues`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"sub_issue_id\":1}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/issues/{issue_number}/sub_issues');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / PATCH /repos/{owner}/{repo}/issues/{issue_number}/sub_issues/priority", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/sub_issues/priority`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"sub_issue_id\":1}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/issues/{issue_number}/sub_issues/priority');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues/{issue_number}/suggestions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/suggestions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/{issue_number}/suggestions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / POST /repos/{owner}/{repo}/issues/{issue_number}/suggestions/{suggestion_id}/approve", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/suggestions/1/approve`, {
      method: "POST",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/issues/{issue_number}/suggestions/{suggestion_id}/approve');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / POST /repos/{owner}/{repo}/issues/{issue_number}/suggestions/{suggestion_id}/dismiss", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/suggestions/1/dismiss`, {
      method: "POST",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/issues/{issue_number}/suggestions/{suggestion_id}/dismiss');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/issues/{issue_number}/timeline", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/issues/sample/timeline`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/issues/{issue_number}/timeline');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/keys", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/keys`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/keys');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/keys", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/keys`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"key\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/keys');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/keys/{key_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/keys/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/keys/{key_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/keys/{key_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/keys/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/keys/{key_id}');
  });
});

describe("issues / GET /repos/{owner}/{repo}/labels", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/labels`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/labels');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / POST /repos/{owner}/{repo}/labels", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/labels`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/labels');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/labels/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/labels/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/labels/{name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / DELETE /repos/{owner}/{repo}/labels/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/labels/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/labels/{name}');
  });
});

describe("issues / PATCH /repos/{owner}/{repo}/labels/{name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/labels/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"new_name\":\"sample\",\"color\":\"sample\",\"description\":\"sample\",\"archived\":true}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/labels/{name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/languages", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/languages`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/languages');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("licenses / GET /repos/{owner}/{repo}/license", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/license`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/license');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/merge-upstream", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/merge-upstream`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"branch\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/merge-upstream');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/merges", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/merges`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"base\":\"sample\",\"head\":\"sample\"}",
    });
    const okCodes = ["201","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/merges');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/milestones", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/milestones`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/milestones');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / POST /repos/{owner}/{repo}/milestones", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/milestones`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"title\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/milestones');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/milestones/{milestone_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/milestones/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/milestones/{milestone_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / DELETE /repos/{owner}/{repo}/milestones/{milestone_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/milestones/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/milestones/{milestone_number}');
  });
});

describe("issues / PATCH /repos/{owner}/{repo}/milestones/{milestone_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/milestones/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"title\":\"sample\",\"state\":\"open\",\"description\":\"sample\",\"due_on\":\"2026-01-01T00:00:00Z\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/milestones/{milestone_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("issues / GET /repos/{owner}/{repo}/milestones/{milestone_number}/labels", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/milestones/sample/labels`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/milestones/{milestone_number}/labels');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /repos/{owner}/{repo}/notifications", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/notifications`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/notifications');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / PUT /repos/{owner}/{repo}/notifications", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/notifications`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"last_read_at\":\"2026-01-01T00:00:00Z\"}",
    });
    const okCodes = ["202","205"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/notifications');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/pages", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pages`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pages');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / PUT /repos/{owner}/{repo}/pages", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pages`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "\"sample\"",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/pages');
  });
});

describe("repos / POST /repos/{owner}/{repo}/pages", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pages`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "\"sample\"",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/pages');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/pages", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pages`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/pages');
  });
});

describe("repos / GET /repos/{owner}/{repo}/pages/builds", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pages/builds`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pages/builds');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/pages/builds", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pages/builds`, {
      method: "POST",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/pages/builds');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/pages/builds/latest", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pages/builds/latest`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pages/builds/latest');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/pages/builds/{build_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pages/builds/1`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pages/builds/{build_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/pages/deployments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pages/deployments`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"pages_build_version\":\"GITHUB_SHA\",\"oidc_token\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/pages/deployments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/pages/deployments/{pages_deployment_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pages/deployments/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pages/deployments/{pages_deployment_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/pages/deployments/{pages_deployment_id}/cancel", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pages/deployments/sample/cancel`, {
      method: "POST",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/pages/deployments/{pages_deployment_id}/cancel');
  });
});

describe("repos / GET /repos/{owner}/{repo}/pages/health", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pages/health`, {
      method: "GET",
    });
    const okCodes = ["200","202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pages/health');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/private-vulnerability-reporting", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/private-vulnerability-reporting`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/private-vulnerability-reporting');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / PUT /repos/{owner}/{repo}/private-vulnerability-reporting", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/private-vulnerability-reporting`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/private-vulnerability-reporting');
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/private-vulnerability-reporting", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/private-vulnerability-reporting`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/private-vulnerability-reporting');
  });
});

describe("repos / GET /repos/{owner}/{repo}/properties/values", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/properties/values`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/properties/values');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / PATCH /repos/{owner}/{repo}/properties/values", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/properties/values`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"properties\":[\"sample\"]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/properties/values');
  });
});

describe("pulls / GET /repos/{owner}/{repo}/pulls", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pulls');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / POST /repos/{owner}/{repo}/pulls", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"head\":\"sample\",\"base\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/pulls');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / GET /repos/{owner}/{repo}/pulls/comments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/comments`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pulls/comments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / GET /repos/{owner}/{repo}/pulls/comments/{comment_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/comments/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pulls/comments/{comment_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / DELETE /repos/{owner}/{repo}/pulls/comments/{comment_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/comments/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/pulls/comments/{comment_id}');
  });
});

describe("pulls / PATCH /repos/{owner}/{repo}/pulls/comments/{comment_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/comments/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"body\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/pulls/comments/{comment_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("reactions / GET /repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/comments/sample/reactions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("reactions / POST /repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/comments/sample/reactions`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"content\":\"+1\"}",
    });
    const okCodes = ["200","201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("reactions / DELETE /repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions/{reaction_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/comments/sample/reactions/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions/{reaction_id}');
  });
});

describe("pulls / GET /repos/{owner}/{repo}/pulls/{pull_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pulls/{pull_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / PATCH /repos/{owner}/{repo}/pulls/{pull_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"title\":\"sample\",\"body\":\"sample\",\"state\":\"open\",\"base\":\"sample\",\"maintainer_can_modify\":true}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/pulls/{pull_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / POST /repos/{owner}/{repo}/pulls/{pull_number}/codespaces", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/codespaces`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"location\":\"sample\",\"geo\":\"EuropeWest\",\"client_ip\":\"sample\",\"machine\":\"sample\",\"devcontainer_path\":\"sample\",\"multi_repo_permissions_opt_out\":true,\"working_directory\":\"sample\",\"idle_timeout_minutes\":1,\"display_name\":\"sample\",\"retention_period_minutes\":1}",
    });
    const okCodes = ["201","202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/pulls/{pull_number}/codespaces');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / GET /repos/{owner}/{repo}/pulls/{pull_number}/comments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/comments`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pulls/{pull_number}/comments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / POST /repos/{owner}/{repo}/pulls/{pull_number}/comments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/comments`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"body\":\"sample\",\"commit_id\":\"sample\",\"path\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/pulls/{pull_number}/comments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / POST /repos/{owner}/{repo}/pulls/{pull_number}/comments/{comment_id}/replies", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/comments/sample/replies`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"body\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/pulls/{pull_number}/comments/{comment_id}/replies');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / GET /repos/{owner}/{repo}/pulls/{pull_number}/commits", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/commits`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pulls/{pull_number}/commits');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / GET /repos/{owner}/{repo}/pulls/{pull_number}/files", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/files`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pulls/{pull_number}/files');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / GET /repos/{owner}/{repo}/pulls/{pull_number}/merge", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/merge`, {
      method: "GET",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pulls/{pull_number}/merge');
  });
});

describe("pulls / PUT /repos/{owner}/{repo}/pulls/{pull_number}/merge", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/merge`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"commit_title\":\"sample\",\"commit_message\":\"sample\",\"sha\":\"sample\",\"merge_method\":\"merge\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/pulls/{pull_number}/merge');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / PUT /repos/{owner}/{repo}/pulls/{pull_number}/merge-async", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/merge-async`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"commit_title\":\"sample\",\"commit_message\":\"sample\",\"sha\":\"sample\",\"merge_method\":\"merge\",\"merge_action\":\"default\"}",
    });
    const okCodes = ["200","202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/pulls/{pull_number}/merge-async');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / GET /repos/{owner}/{repo}/pulls/{pull_number}/merge-async/{uuid}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/merge-async/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pulls/{pull_number}/merge-async/{uuid}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / GET /repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/requested_reviewers`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / POST /repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/requested_reviewers`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "\"sample\"",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / DELETE /repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/requested_reviewers`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"reviewers\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/reviews`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / POST /repos/{owner}/{repo}/pulls/{pull_number}/reviews", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/reviews`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"commit_id\":\"sample\",\"body\":\"sample\",\"event\":\"APPROVE\",\"comments\":[{\"path\":\"sample\",\"body\":\"sample\"}]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/pulls/{pull_number}/reviews');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/reviews/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / PUT /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/reviews/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"body\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / DELETE /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/reviews/sample`, {
      method: "DELETE",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/comments", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/reviews/sample/comments`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/comments');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / PUT /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/dismissals", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/reviews/sample/dismissals`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"message\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/dismissals');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / POST /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/events", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/reviews/sample/events`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"event\":\"APPROVE\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/events');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / PUT /repos/{owner}/{repo}/pulls/{pull_number}/update-branch", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/pulls/sample/update-branch`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"expected_head_sha\":\"sample\"}",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/pulls/{pull_number}/update-branch');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/readme", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/readme`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/readme');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/readme/{dir}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/readme/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/readme/{dir}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/releases", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/releases`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/releases');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/releases", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/releases`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"tag_name\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/releases');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/releases/assets/{asset_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/releases/assets/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/releases/assets/{asset_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/releases/assets/{asset_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/releases/assets/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/releases/assets/{asset_id}');
  });
});

describe("repos / PATCH /repos/{owner}/{repo}/releases/assets/{asset_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/releases/assets/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"label\":\"sample\",\"state\":\"\\\"uploaded\\\"\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/releases/assets/{asset_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/releases/generate-notes", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/releases/generate-notes`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"tag_name\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/releases/generate-notes');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/releases/latest", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/releases/latest`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/releases/latest');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/releases/tags/{tag}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/releases/tags/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/releases/tags/{tag}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/releases/{release_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/releases/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/releases/{release_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/releases/{release_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/releases/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/releases/{release_id}');
  });
});

describe("repos / PATCH /repos/{owner}/{repo}/releases/{release_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/releases/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"tag_name\":\"sample\",\"target_commitish\":\"sample\",\"name\":\"sample\",\"body\":\"sample\",\"draft\":true,\"prerelease\":true,\"make_latest\":\"true\",\"discussion_category_name\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/releases/{release_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/releases/{release_id}/assets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/releases/sample/assets`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/releases/{release_id}/assets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/releases/{release_id}/assets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/releases/sample/assets?name=sample`, {
      method: "POST",
      headers: { 'content-type': "application/octet-stream" },
      body: "\"sample\"",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/releases/{release_id}/assets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("reactions / GET /repos/{owner}/{repo}/releases/{release_id}/reactions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/releases/sample/reactions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/releases/{release_id}/reactions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("reactions / POST /repos/{owner}/{repo}/releases/{release_id}/reactions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/releases/sample/reactions`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"content\":\"+1\"}",
    });
    const okCodes = ["200","201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/releases/{release_id}/reactions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("reactions / DELETE /repos/{owner}/{repo}/releases/{release_id}/reactions/{reaction_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/releases/sample/reactions/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/releases/{release_id}/reactions/{reaction_id}');
  });
});

describe("repos / GET /repos/{owner}/{repo}/rules/branches/{branch}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/rules/branches/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/rules/branches/{branch}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/rulesets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/rulesets`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/rulesets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/rulesets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/rulesets`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"enforcement\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/rulesets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/rulesets/rule-suites", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/rulesets/rule-suites`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/rulesets/rule-suites');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/rulesets/rule-suites/{rule_suite_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/rulesets/rule-suites/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/rulesets/rule-suites/{rule_suite_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/rulesets/{ruleset_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/rulesets/1`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/rulesets/{ruleset_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / PUT /repos/{owner}/{repo}/rulesets/{ruleset_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/rulesets/1`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"target\":\"branch\",\"enforcement\":\"sample\",\"bypass_actors\":[\"sample\"],\"conditions\":\"sample\",\"rules\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/rulesets/{ruleset_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/rulesets/{ruleset_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/rulesets/1`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/rulesets/{ruleset_id}');
  });
});

describe("repos / GET /repos/{owner}/{repo}/rulesets/{ruleset_id}/history", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/rulesets/1/history`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/rulesets/{ruleset_id}/history');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/rulesets/{ruleset_id}/history/{version_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/rulesets/1/history/1`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/rulesets/{ruleset_id}/history/{version_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("secret-scanning / GET /repos/{owner}/{repo}/secret-scanning/alerts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/secret-scanning/alerts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/secret-scanning/alerts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("secret-scanning / GET /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/secret-scanning/alerts/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("secret-scanning / PATCH /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/secret-scanning/alerts/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "\"sample\"",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("secret-scanning / GET /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}/locations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/secret-scanning/alerts/sample/locations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}/locations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("secret-scanning / GET /repos/{owner}/{repo}/secret-scanning/custom-patterns", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/secret-scanning/custom-patterns`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/secret-scanning/custom-patterns');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("secret-scanning / POST /repos/{owner}/{repo}/secret-scanning/custom-patterns", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/secret-scanning/custom-patterns`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"patterns\":[\"sample\"]}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/secret-scanning/custom-patterns');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("secret-scanning / DELETE /repos/{owner}/{repo}/secret-scanning/custom-patterns", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/secret-scanning/custom-patterns`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"patterns\":[\"sample\"]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/secret-scanning/custom-patterns');
  });
});

describe("secret-scanning / PATCH /repos/{owner}/{repo}/secret-scanning/custom-patterns/{pattern_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/secret-scanning/custom-patterns/1`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "\"sample\"",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/secret-scanning/custom-patterns/{pattern_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("secret-scanning / POST /repos/{owner}/{repo}/secret-scanning/push-protection-bypasses", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/secret-scanning/push-protection-bypasses`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"reason\":\"sample\",\"placeholder_id\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/secret-scanning/push-protection-bypasses');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("secret-scanning / GET /repos/{owner}/{repo}/secret-scanning/scan-history", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/secret-scanning/scan-history`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/secret-scanning/scan-history');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("security-advisories / GET /repos/{owner}/{repo}/security-advisories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/security-advisories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/security-advisories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("security-advisories / POST /repos/{owner}/{repo}/security-advisories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/security-advisories`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"summary\":\"sample\",\"description\":\"sample\",\"vulnerabilities\":[{\"package\":{\"ecosystem\":\"sample\"}}]}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/security-advisories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("security-advisories / POST /repos/{owner}/{repo}/security-advisories/reports", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/security-advisories/reports`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"summary\":\"sample\",\"description\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/security-advisories/reports');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("security-advisories / GET /repos/{owner}/{repo}/security-advisories/{ghsa_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/security-advisories/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/security-advisories/{ghsa_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("security-advisories / PATCH /repos/{owner}/{repo}/security-advisories/{ghsa_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/security-advisories/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"summary\":\"sample\",\"description\":\"sample\",\"cve_id\":\"sample\",\"vulnerabilities\":[{\"package\":{\"ecosystem\":\"sample\"}}],\"cwe_ids\":[\"sample\"],\"credits\":[{\"login\":\"sample\",\"type\":\"sample\"}],\"severity\":\"critical\",\"cvss_vector_string\":\"sample\",\"state\":\"published\",\"collaborating_users\":[\"sample\"],\"collaborating_teams\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /repos/{owner}/{repo}/security-advisories/{ghsa_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("security-advisories / POST /repos/{owner}/{repo}/security-advisories/{ghsa_id}/cve", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/security-advisories/sample/cve`, {
      method: "POST",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/security-advisories/{ghsa_id}/cve');
  });
});

describe("security-advisories / POST /repos/{owner}/{repo}/security-advisories/{ghsa_id}/forks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/security-advisories/sample/forks`, {
      method: "POST",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/security-advisories/{ghsa_id}/forks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / GET /repos/{owner}/{repo}/stacks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/stacks`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/stacks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / POST /repos/{owner}/{repo}/stacks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/stacks`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"pull_requests\":[1]}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/stacks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / GET /repos/{owner}/{repo}/stacks/{stack_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/stacks/1`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/stacks/{stack_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / POST /repos/{owner}/{repo}/stacks/{stack_number}/add", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/stacks/1/add`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"pull_requests\":[1]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/stacks/{stack_number}/add');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("pulls / POST /repos/{owner}/{repo}/stacks/{stack_number}/unstack", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/stacks/1/unstack`, {
      method: "POST",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/stacks/{stack_number}/unstack');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /repos/{owner}/{repo}/stargazers", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/stargazers`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/stargazers');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /repos/{owner}/{repo}/stargazers/count", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/stargazers/count`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/stargazers/count');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /repos/{owner}/{repo}/stargazers/history", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/stargazers/history`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/stargazers/history');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/stats/code_frequency", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/stats/code_frequency`, {
      method: "GET",
    });
    const okCodes = ["200","202","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/stats/code_frequency');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/stats/commit_activity", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/stats/commit_activity`, {
      method: "GET",
    });
    const okCodes = ["200","202","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/stats/commit_activity');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/stats/contributors", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/stats/contributors`, {
      method: "GET",
    });
    const okCodes = ["200","202","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/stats/contributors');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/stats/participation", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/stats/participation`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/stats/participation');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/stats/punch_card", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/stats/punch_card`, {
      method: "GET",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/stats/punch_card');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/statuses/{sha}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/statuses/sample`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"state\":\"error\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/statuses/{sha}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /repos/{owner}/{repo}/subscribers", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/subscribers`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/subscribers');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /repos/{owner}/{repo}/subscription", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/subscription`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/subscription');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / PUT /repos/{owner}/{repo}/subscription", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/subscription`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"subscribed\":true,\"ignored\":true}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/subscription');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / DELETE /repos/{owner}/{repo}/subscription", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/subscription`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/subscription');
  });
});

describe("repos / GET /repos/{owner}/{repo}/tags", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/tags`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/tags');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/tarball/{ref}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/tarball/sample`, {
      method: "GET",
    });
    const okCodes = ["302"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/tarball/{ref}');
  });
});

describe("repos / GET /repos/{owner}/{repo}/teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/teams`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/topics", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/topics`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/topics');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / PUT /repos/{owner}/{repo}/topics", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/topics`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"names\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/topics');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/traffic/clones", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/traffic/clones`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/traffic/clones');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/traffic/popular/paths", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/traffic/popular/paths`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/traffic/popular/paths');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/traffic/popular/referrers", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/traffic/popular/referrers`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/traffic/popular/referrers');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/traffic/views", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/traffic/views`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/traffic/views');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /repos/{owner}/{repo}/transfer", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/transfer`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"new_owner\":\"sample\"}",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{owner}/{repo}/transfer');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repos/{owner}/{repo}/vulnerability-alerts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/vulnerability-alerts`, {
      method: "GET",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/vulnerability-alerts');
  });
});

describe("repos / PUT /repos/{owner}/{repo}/vulnerability-alerts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/vulnerability-alerts`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /repos/{owner}/{repo}/vulnerability-alerts');
  });
});

describe("repos / DELETE /repos/{owner}/{repo}/vulnerability-alerts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/vulnerability-alerts`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /repos/{owner}/{repo}/vulnerability-alerts');
  });
});

describe("repos / GET /repos/{owner}/{repo}/zipball/{ref}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/zipball/sample`, {
      method: "GET",
    });
    const okCodes = ["302"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repos/{owner}/{repo}/zipball/{ref}');
  });
});

describe("repos / POST /repos/{template_owner}/{template_repo}/generate", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repos/sample/sample/generate`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /repos/{template_owner}/{template_repo}/generate');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("search / GET /search/code", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/search/code?q=sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /search/code');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("search / GET /search/commits", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/search/commits?q=sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /search/commits');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("search / GET /search/issues", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/search/issues?q=sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /search/issues');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("search / GET /search/labels", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/search/labels?repository_id=1&q=sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /search/labels');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("search / GET /search/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/search/repositories?q=sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /search/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("search / GET /search/topics", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/search/topics?q=sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /search/topics');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("search / GET /search/users", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/search/users?q=sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /search/users');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / GET /teams/{team_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/teams/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /teams/{team_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / DELETE /teams/{team_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/teams/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /teams/{team_id}');
  });
});

describe("teams / PATCH /teams/{team_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/teams/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\"}",
    });
    const okCodes = ["200","201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /teams/{team_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / GET /teams/{team_id}/invitations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/teams/sample/invitations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /teams/{team_id}/invitations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / GET /teams/{team_id}/members", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/teams/sample/members`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /teams/{team_id}/members');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / GET /teams/{team_id}/members/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/teams/sample/members/sample`, {
      method: "GET",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /teams/{team_id}/members/{username}');
  });
});

describe("teams / PUT /teams/{team_id}/members/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/teams/sample/members/sample`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /teams/{team_id}/members/{username}');
  });
});

describe("teams / DELETE /teams/{team_id}/members/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/teams/sample/members/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /teams/{team_id}/members/{username}');
  });
});

describe("teams / GET /teams/{team_id}/memberships/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/teams/sample/memberships/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /teams/{team_id}/memberships/{username}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / PUT /teams/{team_id}/memberships/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/teams/sample/memberships/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"role\":\"member\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /teams/{team_id}/memberships/{username}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / DELETE /teams/{team_id}/memberships/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/teams/sample/memberships/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /teams/{team_id}/memberships/{username}');
  });
});

describe("teams / GET /teams/{team_id}/repos", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/teams/sample/repos`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /teams/{team_id}/repos');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / GET /teams/{team_id}/repos/{owner}/{repo}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/teams/sample/repos/sample/sample`, {
      method: "GET",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /teams/{team_id}/repos/{owner}/{repo}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / PUT /teams/{team_id}/repos/{owner}/{repo}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/teams/sample/repos/sample/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"permission\":\"pull\"}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /teams/{team_id}/repos/{owner}/{repo}');
  });
});

describe("teams / DELETE /teams/{team_id}/repos/{owner}/{repo}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/teams/sample/repos/sample/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /teams/{team_id}/repos/{owner}/{repo}');
  });
});

describe("teams / GET /teams/{team_id}/teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/teams/sample/teams`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /teams/{team_id}/teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /user", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / PATCH /user", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"Omar Jahandar\",\"email\":\"omar@example.com\",\"blog\":\"blog.example.com\",\"twitter_username\":\"therealomarj\",\"company\":\"Acme corporation\",\"location\":\"Berlin, Germany\",\"hireable\":true,\"bio\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /user');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /user/blocks", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/blocks`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/blocks');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /user/blocks/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/blocks/sample`, {
      method: "GET",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/blocks/{username}');
  });
});

describe("users / PUT /user/blocks/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/blocks/sample`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /user/blocks/{username}');
  });
});

describe("users / DELETE /user/blocks/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/blocks/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/blocks/{username}');
  });
});

describe("codespaces / GET /user/codespaces", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/codespaces');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / POST /user/codespaces", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"repository_id\":1}",
    });
    const okCodes = ["201","202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /user/codespaces');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / GET /user/codespaces/secrets", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/secrets`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/codespaces/secrets');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / GET /user/codespaces/secrets/public-key", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/secrets/public-key`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/codespaces/secrets/public-key');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / GET /user/codespaces/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/secrets/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/codespaces/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / PUT /user/codespaces/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/secrets/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"key_id\":\"sample\"}",
    });
    const okCodes = ["201","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /user/codespaces/secrets/{secret_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / DELETE /user/codespaces/secrets/{secret_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/secrets/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/codespaces/secrets/{secret_name}');
  });
});

describe("codespaces / GET /user/codespaces/secrets/{secret_name}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/secrets/sample/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/codespaces/secrets/{secret_name}/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / PUT /user/codespaces/secrets/{secret_name}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/secrets/sample/repositories`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"selected_repository_ids\":[1]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /user/codespaces/secrets/{secret_name}/repositories');
  });
});

describe("codespaces / PUT /user/codespaces/secrets/{secret_name}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/secrets/sample/repositories/1`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /user/codespaces/secrets/{secret_name}/repositories/{repository_id}');
  });
});

describe("codespaces / DELETE /user/codespaces/secrets/{secret_name}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/secrets/sample/repositories/1`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/codespaces/secrets/{secret_name}/repositories/{repository_id}');
  });
});

describe("codespaces / GET /user/codespaces/{codespace_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/codespaces/{codespace_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / DELETE /user/codespaces/{codespace_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/sample`, {
      method: "DELETE",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/codespaces/{codespace_name}');
  });
});

describe("codespaces / PATCH /user/codespaces/{codespace_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"machine\":\"sample\",\"display_name\":\"sample\",\"recent_folders\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /user/codespaces/{codespace_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / POST /user/codespaces/{codespace_name}/exports", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/sample/exports`, {
      method: "POST",
    });
    const okCodes = ["202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /user/codespaces/{codespace_name}/exports');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / GET /user/codespaces/{codespace_name}/exports/{export_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/sample/exports/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/codespaces/{codespace_name}/exports/{export_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / GET /user/codespaces/{codespace_name}/machines", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/sample/machines`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/codespaces/{codespace_name}/machines');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / POST /user/codespaces/{codespace_name}/publish", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/sample/publish`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"private\":false}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /user/codespaces/{codespace_name}/publish');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / POST /user/codespaces/{codespace_name}/start", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/sample/start`, {
      method: "POST",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /user/codespaces/{codespace_name}/start');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("codespaces / POST /user/codespaces/{codespace_name}/stop", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/codespaces/sample/stop`, {
      method: "POST",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /user/codespaces/{codespace_name}/stop');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("packages / GET /user/docker/conflicts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/docker/conflicts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/docker/conflicts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / PATCH /user/email/visibility", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/email/visibility`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"visibility\":\"public\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /user/email/visibility');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /user/emails", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/emails`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/emails');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / POST /user/emails", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/emails`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"emails\":[\"octocat@github.com\",\"mona@github.com\"]}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /user/emails');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / DELETE /user/emails", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/emails`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"emails\":[\"octocat@github.com\",\"mona@github.com\"]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/emails');
  });
});

describe("users / GET /user/followers", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/followers`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/followers');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /user/following", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/following`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/following');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /user/following/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/following/sample`, {
      method: "GET",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/following/{username}');
  });
});

describe("users / PUT /user/following/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/following/sample`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /user/following/{username}');
  });
});

describe("users / DELETE /user/following/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/following/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/following/{username}');
  });
});

describe("users / GET /user/gpg_keys", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/gpg_keys`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/gpg_keys');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / POST /user/gpg_keys", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/gpg_keys`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"armored_public_key\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /user/gpg_keys');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /user/gpg_keys/{gpg_key_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/gpg_keys/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/gpg_keys/{gpg_key_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / DELETE /user/gpg_keys/{gpg_key_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/gpg_keys/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/gpg_keys/{gpg_key_id}');
  });
});

describe("apps / GET /user/installations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/installations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/installations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /user/installations/{installation_id}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/installations/sample/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/installations/{installation_id}/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / PUT /user/installations/{installation_id}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/installations/sample/repositories/sample`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /user/installations/{installation_id}/repositories/{repository_id}');
  });
});

describe("apps / DELETE /user/installations/{installation_id}/repositories/{repository_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/installations/sample/repositories/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/installations/{installation_id}/repositories/{repository_id}');
  });
});

describe("interactions / GET /user/interaction-limits", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/interaction-limits`, {
      method: "GET",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/interaction-limits');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("interactions / PUT /user/interaction-limits", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/interaction-limits`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"limit\":\"sample\"}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /user/interaction-limits');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("interactions / DELETE /user/interaction-limits", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/interaction-limits`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/interaction-limits');
  });
});

describe("issues / GET /user/issues", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/issues`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/issues');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /user/keys", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/keys`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/keys');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / POST /user/keys", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/keys`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"key\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /user/keys');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /user/keys/{key_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/keys/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/keys/{key_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / DELETE /user/keys/{key_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/keys/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/keys/{key_id}');
  });
});

describe("apps / GET /user/marketplace_purchases", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/marketplace_purchases`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/marketplace_purchases');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /user/marketplace_purchases/stubbed", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/marketplace_purchases/stubbed`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/marketplace_purchases/stubbed');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /user/memberships/orgs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/memberships/orgs`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/memberships/orgs');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /user/memberships/orgs/{org}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/memberships/orgs/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/memberships/orgs/{org}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / PATCH /user/memberships/orgs/{org}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/memberships/orgs/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"state\":\"active\"}",
    });
    const okCodes = ["200","202"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /user/memberships/orgs/{org}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("migrations / GET /user/migrations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/migrations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/migrations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("migrations / POST /user/migrations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/migrations`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"repositories\":[\"acme/widgets\"]}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /user/migrations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("migrations / GET /user/migrations/{migration_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/migrations/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/migrations/{migration_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("migrations / GET /user/migrations/{migration_id}/archive", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/migrations/sample/archive`, {
      method: "GET",
    });
    const okCodes = ["302","304"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/migrations/{migration_id}/archive');
  });
});

describe("migrations / DELETE /user/migrations/{migration_id}/archive", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/migrations/sample/archive`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/migrations/{migration_id}/archive');
  });
});

describe("migrations / DELETE /user/migrations/{migration_id}/repos/{repo_name}/lock", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/migrations/sample/repos/sample/lock`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/migrations/{migration_id}/repos/{repo_name}/lock');
  });
});

describe("migrations / GET /user/migrations/{migration_id}/repositories", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/migrations/sample/repositories`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/migrations/{migration_id}/repositories');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /user/orgs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/orgs`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/orgs');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("packages / GET /user/packages", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/packages?package_type=npm`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/packages');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("packages / GET /user/packages/{package_type}/{package_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/packages/sample/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/packages/{package_type}/{package_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("packages / DELETE /user/packages/{package_type}/{package_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/packages/sample/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/packages/{package_type}/{package_name}');
  });
});

describe("packages / POST /user/packages/{package_type}/{package_name}/restore", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/packages/sample/sample/restore`, {
      method: "POST",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /user/packages/{package_type}/{package_name}/restore');
  });
});

describe("packages / GET /user/packages/{package_type}/{package_name}/versions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/packages/sample/sample/versions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/packages/{package_type}/{package_name}/versions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("packages / GET /user/packages/{package_type}/{package_name}/versions/{package_version_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/packages/sample/sample/versions/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/packages/{package_type}/{package_name}/versions/{package_version_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("packages / DELETE /user/packages/{package_type}/{package_name}/versions/{package_version_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/packages/sample/sample/versions/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/packages/{package_type}/{package_name}/versions/{package_version_id}');
  });
});

describe("packages / POST /user/packages/{package_type}/{package_name}/versions/{package_version_id}/restore", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/packages/sample/sample/versions/sample/restore`, {
      method: "POST",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /user/packages/{package_type}/{package_name}/versions/{package_version_id}/restore');
  });
});

describe("users / GET /user/public_emails", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/public_emails`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/public_emails');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /user/repos", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/repos`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/repos');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / POST /user/repos", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/repos`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"Team Environment\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /user/repos');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /user/repository_invitations", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/repository_invitations`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/repository_invitations');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / DELETE /user/repository_invitations/{invitation_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/repository_invitations/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/repository_invitations/{invitation_id}');
  });
});

describe("repos / PATCH /user/repository_invitations/{invitation_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/repository_invitations/sample`, {
      method: "PATCH",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /user/repository_invitations/{invitation_id}');
  });
});

describe("users / GET /user/social_accounts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/social_accounts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/social_accounts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / POST /user/social_accounts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/social_accounts`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"account_urls\":[\"https://www.linkedin.com/company/github/\",\"https://twitter.com/github\"]}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /user/social_accounts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / DELETE /user/social_accounts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/social_accounts`, {
      method: "DELETE",
      headers: { 'content-type': "application/json" },
      body: "{\"account_urls\":[\"https://www.linkedin.com/company/github/\",\"https://twitter.com/github\"]}",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/social_accounts');
  });
});

describe("users / GET /user/ssh_signing_keys", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/ssh_signing_keys`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/ssh_signing_keys');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / POST /user/ssh_signing_keys", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/ssh_signing_keys`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"key\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /user/ssh_signing_keys');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /user/ssh_signing_keys/{ssh_signing_key_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/ssh_signing_keys/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/ssh_signing_keys/{ssh_signing_key_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / DELETE /user/ssh_signing_keys/{ssh_signing_key_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/ssh_signing_keys/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/ssh_signing_keys/{ssh_signing_key_id}');
  });
});

describe("activity / GET /user/starred", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/starred`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/starred');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /user/starred/{owner}/{repo}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/starred/sample/sample`, {
      method: "GET",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/starred/{owner}/{repo}');
  });
});

describe("activity / PUT /user/starred/{owner}/{repo}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/starred/sample/sample`, {
      method: "PUT",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /user/starred/{owner}/{repo}');
  });
});

describe("activity / DELETE /user/starred/{owner}/{repo}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/starred/sample/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /user/starred/{owner}/{repo}');
  });
});

describe("activity / GET /user/subscriptions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/subscriptions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/subscriptions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("teams / GET /user/teams", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/teams`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/teams');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /user/{account_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /user/{account_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / POST /user/{user_id}/projectsV2/{project_number}/drafts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/user/sample/projectsV2/sample/drafts`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"title\":\"sample\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /user/{user_id}/projectsV2/{project_number}/drafts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /users", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / POST /users/{user_id}/projectsV2/{project_number}/views", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/projectsV2/sample/views`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"Sprint Board\",\"layout\":\"board\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /users/{user_id}/projectsV2/{project_number}/views');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /users/{username}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / POST /users/{username}/attestations/bulk-list", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/attestations/bulk-list`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"subject_digests\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /users/{username}/attestations/bulk-list');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / POST /users/{username}/attestations/delete-request", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/attestations/delete-request`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"subject_digests\":[\"sample\"]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /users/{username}/attestations/delete-request');
  });
});

describe("users / DELETE /users/{username}/attestations/digest/{subject_digest}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/attestations/digest/sample`, {
      method: "DELETE",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /users/{username}/attestations/digest/{subject_digest}');
  });
});

describe("users / DELETE /users/{username}/attestations/{attestation_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/attestations/1`, {
      method: "DELETE",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /users/{username}/attestations/{attestation_id}');
  });
});

describe("users / GET /users/{username}/attestations/{subject_digest}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/attestations/sample`, {
      method: "GET",
    });
    const okCodes = ["200","201","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/attestations/{subject_digest}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / GET /users/{username}/copilot-spaces", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/copilot-spaces`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/copilot-spaces');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / POST /users/{username}/copilot-spaces", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/copilot-spaces`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"My Development Space\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /users/{username}/copilot-spaces');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / GET /users/{username}/copilot-spaces/{space_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/copilot-spaces/1`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/copilot-spaces/{space_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / PUT /users/{username}/copilot-spaces/{space_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/copilot-spaces/1`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"Updated Development Space\",\"description\":\"Updated personal space for development assistance\",\"general_instructions\":\"Updated instructions to help me with React development patterns and best practices\",\"base_role\":\"reader\",\"resources_attributes\":[{\"resource_type\":\"repository\",\"metadata\":{\"repository_id\":1,\"file_path\":\"sample\",\"text\":\"sample\",\"name\":\"sample\",\"number\":1}}]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /users/{username}/copilot-spaces/{space_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / DELETE /users/{username}/copilot-spaces/{space_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/copilot-spaces/1`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /users/{username}/copilot-spaces/{space_number}');
  });
});

describe("copilot-spaces / GET /users/{username}/copilot-spaces/{space_number}/collaborators", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/copilot-spaces/1/collaborators`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/copilot-spaces/{space_number}/collaborators');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / POST /users/{username}/copilot-spaces/{space_number}/collaborators", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/copilot-spaces/1/collaborators`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"actor_type\":\"User\",\"actor_identifier\":\"sample\",\"role\":\"reader\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /users/{username}/copilot-spaces/{space_number}/collaborators');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / PUT /users/{username}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/copilot-spaces/1/collaborators/User/sample`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"role\":\"reader\"}",
    });
    const okCodes = ["200","204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /users/{username}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / DELETE /users/{username}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/copilot-spaces/1/collaborators/User/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /users/{username}/copilot-spaces/{space_number}/collaborators/{actor_type}/{actor_identifier}');
  });
});

describe("copilot-spaces / GET /users/{username}/copilot-spaces/{space_number}/resources", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/copilot-spaces/1/resources`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/copilot-spaces/{space_number}/resources');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / POST /users/{username}/copilot-spaces/{space_number}/resources", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/copilot-spaces/1/resources`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"resource_type\":\"repository\",\"metadata\":{}}",
    });
    const okCodes = ["200","201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /users/{username}/copilot-spaces/{space_number}/resources');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / GET /users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/copilot-spaces/1/resources/1`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / PUT /users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/copilot-spaces/1/resources/1`, {
      method: "PUT",
      headers: { 'content-type': "application/json" },
      body: "{\"metadata\":{}}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PUT /users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("copilot-spaces / DELETE /users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/copilot-spaces/1/resources/1`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /users/{username}/copilot-spaces/{space_number}/resources/{space_resource_id}');
  });
});

describe("packages / GET /users/{username}/docker/conflicts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/docker/conflicts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/docker/conflicts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /users/{username}/events", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/events`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/events');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /users/{username}/events/orgs/{org}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/events/orgs/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/events/orgs/{org}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /users/{username}/events/public", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/events/public`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/events/public');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /users/{username}/followers", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/followers`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/followers');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /users/{username}/following", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/following`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/following');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /users/{username}/following/{target_user}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/following/sample`, {
      method: "GET",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/following/{target_user}');
  });
});

describe("gists / GET /users/{username}/gists", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/gists`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/gists');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /users/{username}/gpg_keys", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/gpg_keys`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/gpg_keys');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /users/{username}/hovercard", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/hovercard`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/hovercard');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("apps / GET /users/{username}/installation", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/installation`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/installation');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /users/{username}/keys", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/keys`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/keys');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("orgs / GET /users/{username}/orgs", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/orgs`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/orgs');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("packages / GET /users/{username}/packages", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/packages?package_type=npm`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/packages');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("packages / GET /users/{username}/packages/{package_type}/{package_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/packages/sample/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/packages/{package_type}/{package_name}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("packages / DELETE /users/{username}/packages/{package_type}/{package_name}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/packages/sample/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /users/{username}/packages/{package_type}/{package_name}');
  });
});

describe("packages / POST /users/{username}/packages/{package_type}/{package_name}/restore", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/packages/sample/sample/restore`, {
      method: "POST",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /users/{username}/packages/{package_type}/{package_name}/restore');
  });
});

describe("packages / GET /users/{username}/packages/{package_type}/{package_name}/versions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/packages/sample/sample/versions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/packages/{package_type}/{package_name}/versions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("packages / GET /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/packages/sample/sample/versions/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("packages / DELETE /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/packages/sample/sample/versions/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}');
  });
});

describe("packages / POST /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}/restore", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/packages/sample/sample/versions/sample/restore`, {
      method: "POST",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}/restore');
  });
});

describe("projects / GET /users/{username}/projectsV2", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/projectsV2`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/projectsV2');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / GET /users/{username}/projectsV2/{project_number}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/projectsV2/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/projectsV2/{project_number}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / GET /users/{username}/projectsV2/{project_number}/fields", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/projectsV2/sample/fields`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/projectsV2/{project_number}/fields');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / POST /users/{username}/projectsV2/{project_number}/fields", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/projectsV2/sample/fields`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "{\"name\":\"sample\",\"data_type\":\"text\"}",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /users/{username}/projectsV2/{project_number}/fields');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / GET /users/{username}/projectsV2/{project_number}/fields/{field_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/projectsV2/sample/fields/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/projectsV2/{project_number}/fields/{field_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / GET /users/{username}/projectsV2/{project_number}/items", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/projectsV2/sample/items`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/projectsV2/{project_number}/items');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / POST /users/{username}/projectsV2/{project_number}/items", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/projectsV2/sample/items`, {
      method: "POST",
      headers: { 'content-type': "application/json" },
      body: "\"sample\"",
    });
    const okCodes = ["201"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for POST /users/{username}/projectsV2/{project_number}/items');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / GET /users/{username}/projectsV2/{project_number}/items/{item_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/projectsV2/sample/items/sample`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/projectsV2/{project_number}/items/{item_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / DELETE /users/{username}/projectsV2/{project_number}/items/{item_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/projectsV2/sample/items/sample`, {
      method: "DELETE",
    });
    const okCodes = ["204"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for DELETE /users/{username}/projectsV2/{project_number}/items/{item_id}');
  });
});

describe("projects / PATCH /users/{username}/projectsV2/{project_number}/items/{item_id}", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/projectsV2/sample/items/sample`, {
      method: "PATCH",
      headers: { 'content-type': "application/json" },
      body: "{\"fields\":[{\"id\":1,\"value\":\"sample\"}]}",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for PATCH /users/{username}/projectsV2/{project_number}/items/{item_id}');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("projects / GET /users/{username}/projectsV2/{project_number}/views/{view_number}/items", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/projectsV2/sample/views/sample/items`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/projectsV2/{project_number}/views/{view_number}/items');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /users/{username}/received_events", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/received_events`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/received_events');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /users/{username}/received_events/public", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/received_events/public`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/received_events/public');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("repos / GET /users/{username}/repos", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/repos`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/repos');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("billing / GET /users/{username}/settings/billing/ai_credit/usage", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/settings/billing/ai_credit/usage`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/settings/billing/ai_credit/usage');
  });
});

describe("billing / GET /users/{username}/settings/billing/premium_request/usage", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/settings/billing/premium_request/usage`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/settings/billing/premium_request/usage');
  });
});

describe("billing / GET /users/{username}/settings/billing/usage", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/settings/billing/usage`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/settings/billing/usage');
  });
});

describe("billing / GET /users/{username}/settings/billing/usage/summary", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/settings/billing/usage/summary`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/settings/billing/usage/summary');
  });
});

describe("users / GET /users/{username}/social_accounts", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/social_accounts`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/social_accounts');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("users / GET /users/{username}/ssh_signing_keys", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/ssh_signing_keys`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/ssh_signing_keys');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /users/{username}/starred", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/starred`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/starred');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("activity / GET /users/{username}/subscriptions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/users/sample/subscriptions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /users/{username}/subscriptions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("meta / GET /versions", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/versions`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /versions');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("application/"), 'expected application/json, got ' + ct.split(';')[0]);
  });
});

describe("meta / GET /zen", () => {
  test('responds per spec', async () => {
    const res = await fetch(`${BASE}/zen`, {
      method: "GET",
    });
    const okCodes = ["200"];
    assert.ok(okCodes.includes(String(res.status)) || (okCodes.includes('2xx') && res.status >= 200 && res.status < 300),
      'expected status ' + okCodes.join('/') + ', got ' + res.status + ' for GET /zen');
    const ct = res.headers.get('content-type') || '';
    assert.ok(ct.startsWith("text/"), 'expected text/plain, got ' + ct.split(';')[0]);
  });
});

